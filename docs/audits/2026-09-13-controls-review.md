# 控件与交互检查

范围：当前简化分支的地图工具栏、工作区卡片、分支列表、commit/历史节点和详情操作。沿用 Impeccable 的 Operate 界面规范；保留地图视觉语言和人工验收政策，不改动存储协议。

首轮实际页面收集了 174 个控件实例（含隐藏或重复实例，不等于 174 项操作全部验收）。`target/verification/button-audit-eImvWC` 保存截图和清单；实际重现了复制后的 Copied 在 1.6 秒内被定时重绘改回 Copy。

| 控件类别 | 检查与修改 |
|---|---|
| Branch / HEAD / Worktree / hash / task ID 复制 | 共用复制状态；等待中阻止重复提交；成功/失败反馈跨重绘保留 2.4 秒后恢复；失败保留原值可选中复制 |
| 工作区详情、更多聊天、历史展开 | 工作区展开按钮明确绑定内容区；主要卡片按钮 44 px；保留自然高度、地图统一滚动和折叠状态 |
| commit 节点 | 图形不变，点击区域从 24 px 扩到 44 px；圆形 hover/选中边界；打开后让节点可见并将键盘焦点转入详情操作，关闭后返回原节点并清除旧选中标记 |
| 分支列表 | 从仅定位改为定位并查看该 ref 的 HEAD，标签同步说明 Inspect reference |
| 详情 Close / Collapse / Expand / Larger / Smaller | 保留独立关闭、折叠和调整尺寸语义；键盘焦点与 aria 状态纳入检查 |
| 缩放、方向、定位、平移、工作区搜索 | 保留现有语义，纳入实际页面操作复核 |
| 禁用与忙碌状态 | 普通禁用使用 not-allowed；只有 aria-busy 使用 progress，避免所有禁用按钮都像在等待 |
| 刷新、任务导航、计划与边界详情 | 既有渲染契约覆盖成功/失败/过期/不完整状态；不点击外部任务或发送跨任务消息来冒充实际宿主端到端验收 |

106 项渲染/交互检查通过，新增了复制在重绘中持续、重复请求只写一次、关闭 commit 详情后的焦点返回检查。几何与单层滚动使用此前 97 项几何检查及实际页面记录，未修改其算法。构件、实际浏览器操作复核与范围限制保存在本轮 preview 目录；尚未得到用户最终人工验收结论。

实际浏览器确认：420 和 900 px 两种宽度均通过复制反馈跨刷新保留及复位、三种字段复制到剪贴板、Enter/Space 展开收起、缩放/方向/定位/平移、分支 HEAD 详情及 hash 复制、详情尺寸/折叠/关闭、搜索关闭、44 px commit 点击区域和焦点返回。页面无脚本异常，HTML 200662 字节（未放宽 196 KiB 上限）。精确复制值和拒绝剪贴板访问的分支由渲染测试覆盖；真实浏览器检查读取剪贴板非空，不把外部任务导航或全部 174 个实例计为实际验收。

证据：`target/verification/button-final/browser-check.json`、`confirmed-420.png`、`confirmed-900.png`。首次浏览器检查立即读取异步复制结果，读到了等待状态；改用等待 Copied 后复测通过，未因此修改产品代码。

构件：`target/verification/button-final/devmap.exe`，9905152 字节；SHA-256 `E09CC377F34B5328A66A1048F7B47F8997F7742F0DE830533B2AC0DF909A54B5`。源代码为 `codex/devmap-sqlite-compatible` / `ac24f14` 加本次未提交改动。预览控制器保持运行供人工体验；没有合并、推送或安装。
