# Query path milestone: compatibility passed, performance still open

The read-only shared executor retains a physically sealed source and a bounded, rechecked plain Git configuration proof. Failed connection-time physical capture is permanently refused for queries. Mutations and inventory acceptance retain their independent fresh validation. The frontend assets, wire schemas, Git observation timestamps and production two-second Git cache age are unchanged.

Nine query regressions passed, including missing global configuration creation, byte-identical configuration replacement, worktreeConfig fallback, HEAD movement/expiry, moved/recreated sources and failed connection seal repair. Four process/application integration targets passed 31 tests. Renderer/core/packaging JavaScript tests passed 205 tests. Bounded independent source review found no remaining blocker. These checks do not constitute the final whole-branch or performance acceptance.

## Real Release smoke

Candidate executable SHA-256: `ed78a4dba5e928066193312899f7afbbc4058467f8c576c5b48856436b6d5bc4`. Built from `988663b` plus this query-validation milestone. All runs used explicitly owned fixtures; installed runtimes and user repositories were untouched.

| Scope | Observed result | Gate |
| --- | --- | --- |
| Tiny owner-cold, two observations | 3910 / 3841 ms | 3000 ms, failed |
| Tiny four-client hot subset, eight observations | approximately 197–812 ms | 200 ms per-client p95, failed |
| Tiny freshness, four measured changes / 16 detections | max-of-four p95 3787 ms | 2000 ms, failed |
| Complete summary result | maximum 5431 bytes | 32768 bytes, passed in this smoke |

The tiny fixture contains two real worktrees, two sessions and six journal records. Runs `shared-summary-S24xdI` and `shared-freshness-Qn5ja8` completed response/cursor, full SQL/backup preservation and owned cleanup checks. They are not substitutes for the fixed scale populations.

Earlier failed run `shared-summary-t48DR0` remains retained. Its second query spanned approximately 70 minutes of Modern Standby, verified against Windows System Kernel-Power events 506/507. The first, pre-standby cold observation was 6125 ms. No failure was deleted from its report.

## Optimized in-process diagnostic

The exact-receipt ignored profiler uses a controlled 60-second hot window solely to isolate costs. It does not measure production TTL, transport latency or concurrent clients. Stages are independent calls, not additive spans of one request. The scale fixture contains 20 real worktrees, 100 sessions and 100000 records; its public host-observed task count is zero, distinct from stored task entries.

Run `query-stage-LkE6Ca`, optimized test executable SHA-256 `acdc18a7c39fc047be64d71782c5ee1cb825bab52fd7dd2eb75d74cd20143fc7`, completed five samples per repeated stage and preserved all SQL tables and backups. The owned Windows Job ended empty with root exit code zero.

| Stage | Mean milliseconds | Git starts per call |
| --- | ---: | ---: |
| Connection physical validation | 1.29 | 0 |
| Source physical validation | 1.17 | 0 |
| Configuration witness recheck | 3.79 | 0 |
| SQLite existing-store open | 1.48 | 0 |
| Activation/provenance validation | 7.07 | 0 |
| One current-origin enumeration | 162.30 | 1 |
| Full legacy inventory and hashes | 342.96 | 0 |
| Full active-origin observation | 635.59 | 2 |
| Complete sealed hot query | 710.08 | 2 |

Initial sealed query took 8096 ms and 66 Git starts. Independent cold storage read took 2453 ms; later reads still required origin and legacy-byte validation. The first diagnostic deliberately exposed the mistaken zero-Git assumption on active SQL and failed; the diagnostic now records actual counts while retaining model/cycle/time assertions. The native plain-fixture zero-Git regression remains narrower evidence.

The remaining work is to reduce repeated origin enumeration and full legacy scan cost while retaining moved/replaced/unavailable-origin behavior, malformed-input errors and byte-integrity checks. Do not infer that removing two Git starts alone meets the gate. Final scale cold/hot/freshness/resource populations, final candidate browser/host checks, the complete Rust suite and whole-branch review remain open.

## Bounded streaming inventory follow-up

Inventory hashing now reads through a 64 KiB buffer and checks actual bytes against the remaining aggregate budget. The prior metadata-only precheck could admit a file that subsequently grew beyond that budget. Two real-file boundary/growth tests first failed against the extracted previous behavior; all five Windows controls now pass, including hash equivalence, hard-link rejection, interrupted/short reads and error propagation. Other migration reads retain their existing implementation. All 36 migration/origin/move integration tests passed and bounded source review approved this slice.

Optimized scale run `query-stage-R7rhTH` (test SHA-256 `f49b1fe65a5123fdfad88675920c5faf2910e6db4f7fd75d27f4a4b4ab23c431`) preserved SQL and backups and ended with an empty owned Job. It does not establish a latency improvement: complete inventory averaged 364 ms and sealed hot query 719 ms. Independent file-only diagnostics measured approximately 206 ms for checked opens, 17 ms for reads and 45 ms for SHA updates/finalization. These measurements identify filesystem work as a substantial cost; they do not replace the full production timing population. The bounded buffer is an implementation bound, not a measured whole-process RSS result.

## Discovery dependency correction

A later real native-fixture regression exposed a missing dependency in the first query proof: moving the owned `objects` directory made fresh Git discovery fail, while a sealed hot query still returned its cached cycle. That RED is retained in `task6-query-objects-red.log`; the earlier milestone's narrower tests did not cover this case. Git's discovery scaffold is documented in [Git 2.45.1 setup.c](https://github.com/git/git/blob/v2.45.1/setup.c#L322-L369), and the local executable reports `2.45.1.windows.1`.

The query proof now includes accessible objects/refs directories and their physical identities, common/admin HEAD and packed refs, and a bounded complete loose-ref tree. Every recheck rebuilds the filesystem evidence, so directory additions and byte-identical physical replacements invalidate it. A fresh authoritative inspector runs after candidate capture and before its final recheck; unsupported or changed evidence retains the original fresh path. Config plus source-file evidence retains the combined 4 MiB byte bound. This is not an object-content integrity scanner or an alternative Git ref parser.

All 16 query regressions passed, including refs/HEAD removal, malformed HEAD/current ref, and objects/HEAD replacement. A valid commit invalidates the source proof and triggers fresh validation; an immediately subsequent unchanged call again starts no Git process in the native fixture. Original model/cycle/time assertions and real TTL expiry remain intact. These counts must not be generalized to active SQLite, whose separate origin observation still performs Git enumeration. The previous Release performance numbers predate this correction and are not acceptance for the final candidate.

The correction also passed all 31 process/application integration tests (origin reanchor 3, MCP 11, repository application 10, shared application 7), recorded in task6-query-discovery-integration.log. The serial runner exited zero. cargo fmt --check and cargo clippy --all-targets -- -D warnings passed. Independent bounded source review found no remaining blocker for this ordinary-layout proof; final whole-branch and performance acceptance remain open.

## Current discovery-corrected Release smoke

Release a8baef7, SHA-256 65efc63acbc6a6306c0e2db8aeb5baf5471f4d8a4706c6aea3dd30d62d8249de, built successfully in task6-discovery-release.log. Tiny owned run shared-summary-HaTwRQ completed all request, cursor, SQL/backup preservation and cleanup checks with no errors (runner exit zero). Two cold observations were 4408/4560 ms; eight hot observations were approximately 230-934 ms, with per-client p95 254/934/769/703 ms. Complete result maximum was 5431 bytes. Cold and hot gates remain false. This is a smoke baseline for the corrected proof, not the fixed scale population or evidence of improvement. Frontend asset hashes remained identical.

The next origin-enumeration slice has a retained actual RED: both main and linked active SQLite repeat reads start two Git processes, where the proposed reuse test requires zero. Full legacy-byte drift rejection already passes. The first fixture attempt lacked presence records and failed before measuring Git; that failed setup log is retained separately and is not the performance RED. No performance gain is established by adding these tests.

## Resumed origin-enumeration cache validation (2026-09-11)

The retained implementation initially did not compile because its child module used the wrong relative path. After fixing that path, both main and linked repeat-read tests still failed with seven Git starts: diagnostic evidence showed the physically identical main workspace used a Windows extended canonical path in the proof and Git's ordinary path spelling in the scan. The proof now compares checked canonical root paths while returning the original Git-scanned origins unchanged.

Five focused tests passed (task6-origin-cache-first-differential.log): active SQL main/linked repeat reads start zero Git processes, legacy-byte drift still fails, a new worktree invalidates and matches a fresh reader, and non-UTF8 locked metadata retains the fresh scanner error then reacquires after repair. Six integration tests passed in task6-origin-cache-move-integration.log, covering application reanchor, same/foreign old-path occupants, native/frozen moves and frozen-byte tamper. Formatting and warnings-denied all-target Clippy passed after collapsing one nested conditional.

This slice remains uncommitted and has no current Release performance measurement or independent approval. The broader invalidation matrix, explicit aggregate path-evidence bound, between-guard drift control, full branch suite and final scale/browser/host acceptance remain open. Zero Git in an isolated storage repeat read is not a latency or whole-query acceptance claim.

## Origin-cache boundary validation and measurement

The explicit 1 MiB path-evidence budget first failed its exhausted-budget test, then passed with checked admission before recording directory or missing-file evidence. Nine focused tests now cover a real worktree addition between guards with refusal, cache discard and successful reacquisition; direct original uncached observer comparison after another linked worktree's HEAD changes; and main/linked/main switching with complete presence, journal, route and binding input equality. Independent source review approved this bounded slice. Seventeen application/shared-runtime integration tests also passed before the repeated-proof checks were consolidated.

The first optimized scale run, query-stage-XmJGXc (test executable SHA-256 d03a29a60320e5bb3b7a2cbb12b1bdebe55876fb900495885528b3a5b19d3264), preserved SQL/backup state and finished with an empty owned Job. It measured zero Git starts but a 778 ms mean sealed hot query, with warm storage reads about 673-709 ms. This did not demonstrate a latency improvement. Four full filesystem-proof checks per hot storage read were then consolidated into the entry and final-return checks of the same immutable proof. Original full inventory hashing and unavailable-path checks remain. Provider clones are not independent live scans. Neither version is an atomic filesystem snapshot; fewer samples can miss some transient changes that are restored before final validation, so this is not a claim of identical rejection at every possible instant. The nine focused controls passed again (47.85 seconds), including persistent between-guard change rejection, and formatting/all-target Clippy passed. Final multi-client and branch acceptance remain open.

The two-boundary optimized run query-stage-jq1B94 (test executable SHA-256 80eba4c629f1991482713e19c6e414ae1210150d2683981bc1eed4bfae7be570) completed with no errors, an empty owned Job and full SQL/backup preservation. Five hot observations averaged 557 ms with zero Git starts, versus 778 ms in the preceding four-check diagnostic; warm storage reads were 464-485 ms. Independent full legacy inventory remained approximately 278 ms, and the original uncached full observer averaged 566 ms. The single initial query measured 9166 ms / 73 Git starts. This is a roughly 28 percent reduction in the diagnostic hot mean, not the required production four-client p95 population or cold acceptance. Source proof and configuration still require filesystem work; removing subprocesses did not remove that cost.

## Full-suite result and retained scale failure

The origin-cache slice was committed as 31784e8. Its complete serial `cargo test --all-targets --no-fail-fast -- --test-threads=1` run finished with exit code 101: 552 passed, one failed and eight ignored. The sole failure is `budget_preserves_all_256_real_workspaces_and_late_dirty_unprotected_facts`, which returned `GitProcess(Deadline)` at tests/dock_model.rs:1310 before model-budget assertions. All SQLite migration, identity, move, concurrency and startup-activation targets passed. The full log is retained as task6-all-targets-31784e8.log; this is not a passing branch result.

An isolated rerun of the same compiled test reproduced the same deadline failure in 68.35 seconds, including fixture construction. Its eligible Git Trace2 sink is retained in task6-256-trace-31784e8.jsonl alongside task6-256-isolated-31784e8.log. Excluding worktree setup, the final refresh begins at 15:33:28.360 UTC; 256 status commands start between 15:33:32.303 and 15:33:45.645. Shared-proof acquisition proceeds afterward, and a merge-base starts at 15:33:53.237, near the original 25-second deadline. Trace records cannot alone attribute intervals outside Git to admission, process startup, cleanup or filesystem work. No operation budget, fixture population or acceptance threshold has been relaxed. Phase attribution and a verified fix remain open, along with final performance and browser/host acceptance.

An opt-in, test-only stage profiler then completed ten sequential real `git --version` calls with valid output and exit status. Mean total time was 96.95 ms: suspended process creation 5.33 ms, Job attachment/thread discovery/resume 38.13 ms, root wait with concurrent pipe reads 39.76 ms, and cleanup 13.41 ms. Reactor creation/destruction together averaged 0.24 ms. These are Windows debug diagnostic measurements, not scale latency acceptance or attribution of every millisecond inside attachment. The inclusive total and outer measurements must not be added to their component spans. Error-path span labels do not establish successful cleanup, and the captured profile covers the explicit reactor thread, not arbitrary application workers.

The profiler and eight existing process fault regressions passed; formatting, warnings-denied all-target Clippy and independent bounded source review passed. The diagnostic adds no production profiling behavior. Its raw samples and statistics are retained in task6-git-process-stages-first.log and task6-git-process-stages-first-statistics.json. The production fix remains pending.

## Target-process thread discovery candidate

The Windows candidate retains suspended creation and assignment to a kill-on-close Job before execution. It replaces global thread enumeration with [PssCaptureSnapshot](https://learn.microsoft.com/en-us/windows/win32/api/processsnapshot/nf-processsnapshot-psscapturesnapshot) of the held child handle, capturing only thread records. Bounded enumeration requires one live candidate; the opened thread must match the process and creation time before resume. Capture unavailability retains the original Toolhelp path; later identity, resource, release or resume failures do not retry another path. This is not an older-Windows loader fallback claim. [PssFreeSnapshot](https://learn.microsoft.com/en-us/windows/win32/api/processsnapshot/nf-processsnapshot-pssfreesnapshot) uses the current process for the locally captured snapshot.

The retained test-first run had one passing fallback control and four expected failures. The first implemented run passed all five owned-child controls; eight original process regressions also passed. In the same ten-command diagnostic, mean attachment fell from 38.13 ms to 0.165 ms, and total command time from 96.95 ms to 62.24 ms. Cleanup averaged 20.70 ms versus 13.41 ms previously, so no cleanup improvement is claimed. Raw logs are task6-pss-first-green.log, task6-pss-process-regressions.log and task6-git-process-stages-pss.log.

The unchanged 256-worktree case then passed in 47.31 seconds including fixture construction, with the original 25-second refresh budget and all model assertions intact (task6-256-pss-first.log). This is one isolated regression pass, not a new passing whole-suite or production performance population. Independent review found no source blocker and requested additional opened-handle identity controls before the milestone is finalized. Final whole-branch, four-client and UI acceptance remain open.

The additional controls now pass: opened-thread owner/creation mismatch and controlled marker/snapshot release refusal. All nine PSS tests and the owner-quarantine regression passed, as did formatting, warnings-denied all-target Clippy and the final bounded source review. Failure controls use actual owned child processes and assert exact errors and no fixture execution after reaping. Release and resume failures are controlled branches, not observed Windows API failures; these tests do not independently count successful snapshot frees on all error paths. Logs: task6-pss-identity-controls.log, task6-pss-quarantine.log and task6-pss-clippy.log. Whole-suite and production performance acceptance still require the final candidate.

The slice is committed as ad18d83. Its Release executable, SHA-256 654816ddfc3ecff45dd0609f934733d27bb77203636da6fc956456273dc68a58, built successfully and completed tiny real MCP run shared-summary-Q2KPRX with no request or cleanup errors and full SQL/backup and cursor preservation. Two cold observations were 3477/3368 ms. Twelve hot observations had per-client p95 values 459/203/445/461 ms; maximum complete result remained 5431 bytes. Cold and hot gates remain false. This small smoke does not replace the fixed scale population, final freshness cohort, idle resources or UI/host verification. Logs and configuration are task6-pss-release.log, task6-summary-pss-smoke.log and task6-summary-pss-smoke-config.json.

## Cold inspection consolidation

A native main-fixture RED recorded 30 total cold Git starts and two authoritative inspector groups of four commands each. The cold query now captures candidate configuration from authenticated paths, performs one authoritative inspector, compares canonical identities, and only then constructs the application. Proof checks before and after projection, failed-Hello seals and the original unsupported/changed-evidence fallback remain intact. Reordering can change which simultaneous race error is encountered first; this is not identical error-priority timing under every filesystem interleaving.

All 19 query-validation tests passed. Recorded cold starts were main 26, linked 31 and unborn 24; their single inspector groups used 4, 4 and 6 commands respectively. The main observation is a four-command reduction from its actual RED; the other layouts verify one inspector, not a measured before/after total. Warm zero-Git and original cycle/time assertions passed. Objects removed after candidate capture prevent application creation; configuration changed after inspection produces the fresh alternate target and does not retain the stale proof. Formatting, warnings-denied all-target Clippy and independent source review passed.

The retained logs are task6-cold-inspector-red.log and task6-cold-inspector-query-matrix-after-module-fix.log. An initial combined build failed on the separate inventory prototype's module path before running tests; task6-cold-inspector-query-matrix.log records that compiler failure. After correction, the separate parallel-inventory prototype still has its intentional RED (three failed tests, one passing serial-fallback control); that work is not included in this cold-path milestone. The latest Release measurements above precede this consolidation, and all final acceptance gates remain open.

## Bounded parallel inventory milestone

The candidate enumerates and classifies metadata through the original walker, then uses at most four scoped payload readers to compute every full digest. Each reader keeps a 64 KiB buffer. Complete unique ordinals, exact observed byte lengths and aggregate admission limits are required before returning a manifest. Every worker is joined before a decision. Ordinary speculative failures fall back once to the original serial inventory; worker panic returns a controlled error after joining. Stable successful manifests match the serial oracle. Transient failures can be repaired before fallback, so identical error timing across every interleaving is not claimed. The 512 MiB admission limit applies per attempt; fallback can perform another attempt, and failed readers can consume one additional EOF-probe byte each. Safety checks may open additional temporary handles.

Fourteen streaming/parallel controls and all 36 migration, native/frozen origin lifecycle and worktree-move integration cases passed. Controls include actual growth/shrink, hard links, a Windows junction inserted after enumeration, complete manifest equality, quota rejection, duplicate results and panic cleanup. The first duplicate-result fixture accidentally triggered length rejection first; the corrected fixture duplicates the same successful record. The junction fixture initially passed forward-slash paths to mklink and failed before its intended assertion; only its owned fixture path spelling was corrected. All failure logs are retained. Formatting, warnings-denied all-target Clippy and independent bounded source review passed. Actual OS worker-spawn failure and Unix execution were not established by these controls.

Optimized diagnostic query-stage-JnCWRU used test executable SHA-256 763fbc0ba271b907039d1427df24712386b748d44aa14a9f0b74548198038b75 on the fixed 20-worktree, 100-session, 100000-event receipt. Five paired inventories alternated order and compared their entire FrozenManifest against each other and the frozen baseline. Serial inventory averaged 311.820 ms and parallel inventory 150.795 ms, a 51.6 percent reduction for this paired stage. Full sealed hot queries averaged 512.648 ms (493.848-535.418 ms), each with zero Git starts. Application verified aggregates averaged 428.220 ms. The single initial query was 6669.679 ms / 69 Git starts. These are diagnostic observations with the controlled profiling TTL, not the required production four-client p95 population or cold acceptance. The hot target remains unmet.

The owned runner exited zero with an empty Job, no errors, all 14 SQL tables and complete backup inventories preserved. Logs are task6-inventory-parallel-streaming-controls-fixed-path.log, task6-inventory-parallel-migration-regressions.log, task6-inventory-parallel-clippy.log, task6-inventory-parallel-release-profiler-build.log and task6-inventory-parallel-scale-profile.log; statistics are task6-inventory-parallel-scale-statistics.json. This executable is the optimized library test harness, not a rebuilt CLI. Final CLI, complete branch suite, production performance, browser and host acceptance remain open.

## Origin-proof attribution

A test-only extension now independently measures configuration recheck, complete origin evidence reconstruction and full proof recheck. It requires an acquired proof, exact reconstructed evidence equality and successful subsequent revalidation. Actual Git-start counters accompany each stage. No production validation or cache policy changed.

Optimized exact-scale run query-stage-vbaWjV (test executable SHA-256 f7ca264cd22328520ed31351de2d9a95046bf7c24d6b08061dcf4ff22da1d754) completed with no errors, an empty owned Job and full SQL/backup preservation. Five means were configuration 32.576 ms, origin evidence 76.057 ms and complete proof recheck 110.256 ms, all with zero measured Git starts. The observer checks its proof at entry and final return. These independent calls identify repeated filesystem validation as material remaining work; their times are not additive production spans. Full hot queries averaged 492.649 ms (479.207-509.413), still outside the target. The initial query was 6268.133 ms / 69 Git starts, one diagnostic observation.

Formatting and warnings-denied all-target Clippy passed; the optimized build and exact owned diagnostic ran successfully. Retained logs use task6-origin-proof-profiler-counters-* and task6-origin-proof-counters-scale-* prefixes. Earlier query-stage-m38Yvj used static zero annotations for the three new Git fields; it is retained as timing evidence only, superseded by the actual-counter run. No new whole-suite or production acceptance is claimed. Bounded origin-proof concurrency is only a proposed next investigation, with global quota accounting and complete evidence equivalence still to be designed and verified.

## Complete regression result at 98feee7

The serial full command `cargo test --all-targets --no-fail-fast -- --test-threads=1` completed with exit code zero. Across 58 target summaries: 574 passed, zero failed and ten ignored. Summed target test time was 1444.67 seconds; this is not total wall time including compilation. The original 256-worktree budget regression passed within this complete run without changing its budget or assertions. SQLite migration, worktree move, origin replacement, startup activation, shared transport/mutations and existing web/MCP contract targets all completed.

Evidence: task6-all-targets-98feee7.log and task6-all-targets-98feee7-statistics.json. This establishes the complete Rust regression result for 98feee7, before the subsequent proof-overlap patch. It does not establish production performance, final actual browser/host acceptance, Unix execution or overall goal completion. Both frontend asset hashes were rechecked unchanged during this run.

## Overlapping complete origin-proof checks

The candidate retains both observer boundaries and all configuration/evidence checks, but runs serial Evidence capture in one scoped worker while configuration rechecks on the caller. It joins before selecting the configuration-precedence result. Thread creation refusal uses the original serial short circuit; a worker panic cannot authorize success. Configuration remains on the caller's existing Git supervision context. Concurrent read timing differs from the serial implementation, including bounded evidence reads when configuration ultimately declines. No TTL, evidence quota, full legacy hash, schema or response contract changed.

The actual RED run passed two tests and failed four, including the bounded overlap handshake and worker completion assertions. GREEN passed all six without weakening assertions. Fifteen origin-cache controls and 19 query-validation tests passed, followed by 24 store/move/application/shared-runtime integration tests. The first integration command named a nonexistent sqlite_read_only target and exited before running tests; the corrected command used sqlite_store. Both logs are retained. Formatting, warnings-denied all-target Clippy and independent formatted-source review passed. Injected pre-spawn refusal exercises the OS-error fallback branch but does not establish an actual OS thread exhaustion failure. The caller-panic test verifies scoped worker cleanup and original panic payload preservation.

Optimized exact-scale run query-stage-yRwWVi used test executable SHA-256 671fad93a8c6243f0de318d5e62c9b752c75ebfebe1f1f94980d3068ac8d2c4e. Five paired complete-proof calls alternated order: serial mean 100.655 ms, candidate 77.728 ms (22.8 percent lower). Both results had to validate the complete proof. Full hot query mean was 397.370 ms (389.053-411.825), with zero Git starts, still above the target. Application aggregate mean was 328.766 ms. The initial query was 6547.710 ms / 69 Git starts, one observation. These remain controlled-TTL diagnostics, not the production four-client p95 population. No paired whole-query improvement is claimed against earlier runs.

The added metadata-only inventory measurement averaged 57.325 ms and matched the complete captured manifest after clearing expected digests. It follows two full inventory reads, so it is warmed independent evidence, not a subtractable production subspan. Full candidate inventory averaged 137.897 ms. All 14 SQL tables, complete backups and inventory comparisons passed; the owned runner exited zero with an empty Job and no errors.

Logs: task6-proof-overlap-red.log, task6-proof-overlap-first-green.log, task6-proof-overlap-origin-controls.log, task6-proof-overlap-query-controls.log, task6-proof-overlap-integration.log (invalid command), task6-proof-overlap-integration-corrected.log, task6-proof-overlap-clippy.log, task6-proof-overlap-profiler-build.log and task6-proof-overlap-scale-profile.log. Statistics: task6-proof-overlap-scale-statistics.json. The CLI has not been rebuilt for this slice. Final performance, complete final-candidate suite, browser/host and resource acceptance remain open.
## Release browser and actual host checks at ec12bcd

The actual Release CLI rebuilt successfully (task6-ec12bcd-cli-build.log), SHA-256 ef188e7f542119b0b204acf8ed1be95716375ab2b760b499ce10bfdc368a46fd. Browser fixture shared-browser-ekvz5T kept the same MCP process, browser and URL while replacing owner 21832 with 18516. Seven worktrees, six task rows and their observation timestamps survived. Expanded cards/chats, zoom and scroll assertions passed, with recovery and no page errors. This establishes those specific recovery properties, not complete visual acceptance.

Inspection of actual before/after screenshots exposed a test naming error: its previous `selected` field read `aria-expanded`. The harness now calls this `expanded`, retains every original assertion and reports actual aria-current selection separately. The corrected run reports selection_observation.retained=false. Independent focus controls focus-control-AWUzEq and focus-control-q65BnU found that ordinary refresh and owner restart both removed aria-current selection while keyboard focus remained on map-tools-trigger. This is not evidence of a restart-specific regression, nor proof that the previous backend behaves identically. Selection preservation remains an explicit acceptance limitation; no frontend asset was changed. The reporting correction received independent source approval.

The fixed-input browser comparison at task6-ec12bcd-frozen-browser passed all 30 comparisons against source reference 520683a at widths 1280, 560 and 360, with zero visible differing pixels under the existing maximum-channel tolerance of two, no excluded pixels and a passing negative control. Assembled resource SHA-256: 1f2bf0bea4844bf6e6e59a57af3dd7dc1c62507f0f7e66f8c87ebfe007405c36. This is frozen frontend parity, not an old/new backend snapshot comparison or complete lifecycle acceptance.

Actual Codex CLI 0.153.4 ran disposable fixture codex-host-wOXUIw: exactly three successful MCP calls read the map, wrote a route and read it back (task6-ec12bcd-host-smoke.log). A separate recheck passed the wrong-last-readback negative control. Read-only SQL verification found backend_state=active, generation=1 and exactly one persisted route with goal `Actual Codex host acceptance fixture` (task6-ec12bcd-host-sql.json). The candidate binary copy matched the Release SHA. This covers a real CLI MCP roundtrip; desktop navigation, automatic hooks and the full host lifecycle remain outside this check. Installed plugins and user data were not changed. Performance gates, selection behavior, final branch regression and complete visual acceptance remain open.

## Frozen-reference selection control
A follow-up actual Chromium diagnostic used source reference 520683a and the current unchanged frontend with identical frozen inputs. All nine runs (baseline/control/candidate at widths 1280/560/360) changed aria-current selection count from one before refresh to zero afterward; keyboard focus remained on map-tools-trigger. Thirty pixel comparisons still had zero visible differences under the original channel tolerance, and the negative control passed. Thus this specific selection transition is reproduced in the prior frontend, rather than a newly introduced frontend difference. It does not prove old/new live backend lifecycle equivalence or continuous selection retention. No UI change or baseline adjustment was made.

Evidence: task6-920439b-frozen-selection.log and task6-920439b-frozen-selection/report.json, including per-variant selection_controls. The root inspected the baseline desktop before/after screenshots; the selected workspace itself lies outside that viewport after pan, so selection identity is established by the DOM observations rather than visible outline pixels in those two images. The owned diagnostic process exited zero.

## Bounded parallel origin Evidence milestone
The production proof now captures complete origin Evidence through at most four payload workers after bounded serial discovery. The original serial capture remains the oracle and the fresh replay path for spawn refusal or conflicting merges. All handles are joined before deciding; genuine payload errors and worker panics override replay, and missing or malformed completions cannot publish. Complete file bytes, safe opens and reopened identities, reciprocal links, recursive refs, incarnation and original call-based charges remain checked. Discovery limits remain 256 origins and an additional 1 MiB of retained task paths.

A capture-local admission mutex bounds accepted plus pending logical payload bytes to 4 MiB, with 1 MiB per file and unchanged node/path/directory limits. Workers read into at most four separate 8 KiB scratch buffers before reserving each actual chunk for retention. Commit precedes vector length extension; uncommitted reservations refund on drop, while committed charges remain conservative through errors or duplicate map entries. Interrupted reads retry. This is a logical payload bound, not a process RSS, allocator capacity or combined old/new proof memory ceiling. Test observation callbacks are excluded from production.

Actual test-first results were 2 passed / 13 failed for the full collector RED and 0 passed / 15 failed for direct admission RED. Both implemented groups then passed all 15 tests. The complete focused matrix passed 45 origin/cache controls, 19 query controls (one diagnostic ignored), 39 migration/origin/move/reanchor integrations and the unchanged real-256-worktree case: 104 unique tests. The initial 30 direct/collector passes are subsets of the 45 and are not counted twice. The real-256 case finished in 46.95 seconds including fixture setup, retaining its original refresh deadline. Formatting and warnings-denied all-target Clippy passed; the first Clippy run found an unused test-observer trait method, corrected by the same test-only gate as its sole caller. Independent source review approved the implementation and final formatted delta. This is not a final complete-branch suite.

Optimized owned exact-scale run query-stage-KdBJ5H used test executable SHA-256 c61a371cefb84cfd12336fda487cbdb9e2d8636f3888ec1e856af4142f7541f3, built in 28.05 seconds. Five alternating paired captures averaged 71.297 ms for complete serial Evidence and 35.016 ms for the candidate, a 50.9 percent reduction in that stage, with exact complete Evidence equality. Configuration-then-candidate recheck averaged 66.345 ms versus 44.607 ms when overlapping those same operations. Each measured stage had zero Git starts. These independent stage timings are not additive production spans.

Complete sealed hot queries averaged 323.289 ms (316.674-328.386), still above the 200 ms target. Application aggregates averaged 254.480 ms. Full legacy inventory averaged 136.679 ms; its warmed metadata-only diagnostic averaged 58.583 ms. The initial query was 6507.032 ms with 69 Git starts, one observation. These use the diagnostic hot window and do not replace production four-client p95, cold, freshness or resource acceptance. No threshold, fixture population, full hash or production TTL changed.

The owned runner exited zero with an empty Job, no errors and all 14 SQL tables and complete backup inventories preserved. Logs use task6-parallel-admission-first-red/first-green and task6-parallel-evidence-* prefixes; stage statistics are task6-parallel-evidence-scale-statistics.json. This build is the optimized library test harness, not a newly rebuilt CLI. Production performance gates and final whole-branch/browser/host acceptance remain open.

## Configuration component attribution
A diagnostic now times four completed components within the same configuration recheck; production uses a no-op observer and retains the original validation order, short-circuiting, limits and errors. Static component records are buffered until the enclosing timing finishes and the proof is confirmed valid. The components are nested in that total and must not be added to it. No extra capture or configuration recheck was added.

Owned exact-scale run query-stage-4OfGAK used optimized test executable SHA-256 adb20d69c223b00985d30ea61d1f8bb4ea0f54bdf480c4a65dc7ca994d8ce32c. Five enclosing rechecks averaged 32.277 ms: source-resolution capture/comparison 28.666 ms, environment 0.052 ms, directory identities 0.620 ms and configuration-file witnesses 2.928 ms. Actual Git-start deltas were zero in every component. This identifies Git discovery dependencies, chiefly the existing directory/ref evidence path, as the dominant measured part; it does not authorize skipping them or establish which future sharing design is correct.

The Release test build completed in 28.31 seconds and warnings-denied all-target Clippy passed. Independent source review approved the instrumentation. The owned run exited zero with an empty Job, no errors and complete SQL/backup preservation. Full hot queries averaged 327.767 ms (324.352-331.825); this diagnostic remains outside the performance target and does not establish a regression relative to the previous non-paired run. Logs: task6-config-components-clippy.log, task6-config-components-build.log and task6-config-components-scale.log; statistics: task6-config-components-statistics.json. No new CLI, production four-client or whole-branch acceptance is claimed.

## Bounded inventory pipeline candidate (acceptance still open)

The candidate now overlaps the original serial metadata walker with four scoped full-file hash workers. The queue retains at most four small descriptors, with at most four in flight and one held by the producer. Every descriptor passes classification and the checked cumulative metadata-byte reservation before submission; reservations are not recycled. Workers keep the original checked-open/full-hash path and exact expected-length requirement. All workers finish before success, a fatal error, or one original serial retry. Discovery ordinals are validated and merged before restoring canonical output order. These are implementation bounds, not measured whole-process memory or latency claims.

This changes an internal observation-order contract: a valid prefix can be read before a later metadata refusal. First-entry refusal still starts no payload worker. Mutations after complete enumeration can now occur after a particular file was hashed, so historical mutation tests must move their trigger to that actual job's metadata-before-checked-open boundary. The inventory is not an atomic filesystem snapshot; the existing outer before/after validation remains required. Full hashing, byte caps, public error handling through the serial oracle and the original performance gates have not been relaxed.

The reviewed core patch (SHA-256 `08685d2749b078f2f4ac3fa6ea5e45a22d64cecc5585725f3a59e81ff7e50077`) compiled in 10.72 seconds. The owned run `inventory-faults-kpzktN` passed all 14 pipeline controls in 27.06 seconds, including full-queue wakeups, producer/worker panic cleanup, late-prefix admission, actual read/probe counts, missing/invalid ordinals and partial spawn refusal. Its test executable SHA-256 was `f28c6e463057e6401b8ba206e36337fa932c82c9ec4c5bed8b0c92f12c7ce064`; the owned Job exited zero and was empty without timeout or cleanup errors. All-target Clippy passed in 8.27 seconds.

Before historical-test adaptation, the complete inventory module run `inventory-faults-BAjNAe` reported 18 passed and 5 failed in 36.36 seconds. Three failures use the old post-enumeration mutation timing, and two assert no workers before a late quota refusal. These are recorded failures, not a passing full module result. The separate test adaptation, wider migration/query verification and a new retained-scale performance measurement were pending at that checkpoint. This milestone does not establish the 200 ms target, production four-client p95, final browser/host compatibility, or whole-branch acceptance.

Subsequent verification passed all 23 inventory module tests after the reviewed six-test adaptation (39.55 seconds, owned run `inventory-faults-ZC4h0X`, empty Job and no errors). The production core also passed 45 origin/cache tests and 19 query-validation tests, with one intentionally ignored profiling test; these include complete legacy-byte checks and rejection of persistent changes between validation guards. Final adapted all-target Clippy passed in 8.54 seconds. These focused results still do not constitute whole-branch acceptance.

The original optimized baseline executable was preserved before rebuilding, verified as SHA-256 `adb20d69c223b00985d30ea61d1f8bb4ea0f54bdf480c4a65dc7ca994d8ce32c`. The pipeline test executable was built in 29.21 seconds with SHA-256 `2e8842bf6bf871e41b4e0ca7e74a9ff5b6d57c93756daeeb199bd95507fe367c`. Both ran against the unchanged fixed scale receipt in baseline/pipeline/pipeline/baseline order: `query-stage-LYCysc`, `query-stage-90yMSw`, `query-stage-LtrZkH`, `query-stage-1aKY1b`. Every run preserved all 14 SQL tables and complete backup inventories and finished with an empty owned Job, no abort and no errors.

| Diagnostic stage | Baseline mean (ms) | Pipeline mean (ms) | Change |
| --- | ---: | ---: | ---: |
| Complete inventory and hashes | 140.301 | 109.126 | -22.2% |
| Sealed hot query | 323.129 | 290.387 | -10.1% |
| Application verified aggregate | 260.332 | 226.823 | -12.9% |
| Independent serial inventory oracle | 286.488 | 302.919 | +5.7% |
| Warmed metadata-only inventory | 56.852 | 58.322 | +2.6% |
| Configuration recheck | 29.226 | 30.326 | +3.8% |

Each variant has ten observations per repeated stage across its two runs, and these repeated observations recorded zero Git starts. The two hot-query run means were 323.855/322.402 ms for the baseline and 292.360/288.413 ms for the pipeline. The measured gain persisted when run order reversed, but the independent serial-oracle diagnostic was slower and is not hidden by the hot-path improvement. This comparison does not isolate the cause of every small stage difference. Metadata-only timing is independently warmed, not an additive subspan to subtract from full inventory.

The pipeline's two initial-query observations averaged 6200.829 ms with 69 Git starts each. Neither these two initial observations nor ten diagnostic hot observations meet the required production cold/four-client sample protocol. The 200 ms hot target remains unmet, and final cold, freshness, resource, browser/host and whole-branch gates remain open. Statistics and per-run details are retained in `target/verification/task6-inventory-pipeline-statistics.json`; profile logs use the `task6-inventory-pipeline-*-scale` prefix. Wider migration and the original 256-worktree regression are still pending at this checkpoint.

The wider migration matrix subsequently passed all 39 tests: reanchoring 3 (27.31 seconds), migration 20 (64.09 seconds), native-origin lifecycle 5 (40.64 seconds), origin lifecycle 8 (92.36 seconds), and worktree move 3 (30.92 seconds). The unchanged 256-real-workspace integration case passed exactly once in 50.15 seconds, with its original refresh budget and assertions intact. A preceding incorrectly qualified library filter matched zero tests and is excluded from this evidence; the corrected integration-target log explicitly verifies one passed test. Logs are `task6-inventory-pipeline-integration-matrix.log` and `task6-inventory-pipeline-real-256-corrected.log`.

This milestone therefore has 127 distinct passing focused Rust tests (23 inventory + 45 origin/cache + 19 query + 39 integration + 1 real-256 case), plus the separately reported diagnostic comparisons. The 14 pipeline controls are part of the 23 inventory tests, not additional tests. Source, historical-test adaptation and measurement-scope reviews found no remaining milestone blocker. The current CLI has not been rebuilt for this source milestone, and the final whole-branch, live-browser/host and formal performance/resource acceptance remain open.

## Query boundary consolidation: pre-implementation evidence

The pipeline milestone `5fc8878` remains the last production change. The next proposed change combines requester and application-origin validation into entry and post-projection boundaries for an existing active SQL reader with identical canonical source keys and identical retained configuration baselines. Both independent baselines must be compared against fresh evidence. Full origin captures, legacy hashes, pinned SQL checks, connection identity seals, normal Git refresh decisions and the two-second production freshness bound remain required. Distinct keys/baselines and cold or unsupported states retain the original path.

This intentionally widens the closing origin-validation interval. Persistent configuration or ref drift after the former storage-local closing check will now reject the candidate with the origin-proof error, instead of reaching the former requester-only fresh fallback. Stable successful results must remain identical; identical outcomes for every transient or simultaneous-error timing are not claimed. A body error retains its original error and short-circuits closing validation, with all tentative storage and projection caches discarded. Successful candidates require closing validation before publication. No lease may be restored to an invalidated reader.

Actual initial RED, root session 50935 (terminal 101): compilation 12.21 s; one exact test failed in 4.63 s only at the final source-capture assertion, actual 4 versus required 2. Before that assertion, the test passed actual zero-Git, active/nonempty SQL, unchanged database/WAL/legacy/frozen bytes and complete serialized snapshot equality after normalizing only the query evaluation clock. This uses the existing 60-second test harness to isolate cache routing; it is not a production freshness or performance measurement.

The expanded test-only matrix, root session 99478 (terminal 101), compiled in 11.78 s and ran in 115.17 s: **20 passed, 5 intentionally failed, 1 diagnostic ignored**. All 19 pre-existing query tests passed. A new control moving a genuinely authenticated source proof between two same-key harnesses with different retained configuration baselines also passed, requiring the original reacquisition and real Git commands. The five failures were exactly:

- Four actual source captures instead of two.
- Ordinary error after the real storage read left tentative caches populated.
- Caller unwind after the real storage read left tentative caches populated.
- Persistent config drift injected after projection returned a snapshot instead of the proposed closing origin error.
- Persistent ref drift injected after projection returned a snapshot instead of the proposed closing origin error.

Both cache-failure tests exercised successful next-query recovery and data preservation before their final failing cache-discard assertion. They compare the full recovered snapshot after normalizing documented observation clocks and the monotone Git collection cycle, with an independent direct-query target/lane comparison. These caller-unwind tests do not replace existing worker joining and panic tests.

Evidence: `target/verification/task6-query-boundary-initial-red.log` and `target/verification/task6-query-boundary-safety-red.log`. Expanded debug test executable SHA-256: `918F03598F34FDC4F7A80EA83E9FED907FEEB1395FB3BD63A1BB8D45EDB674E7`. The independent source reviews approved the test-only patches before root applied and executed them. These results establish prerequisites, not a completed optimization or full acceptance. The original 200 ms hot-query, cold/freshness, four-client, resource, browser, host and final-suite gates remain open.

### Boundary implementation verification (uncommitted candidate)

Root applied the independently approved five-module implementation patch `B82839A2BAD9683712D25E56AA85FE43A4A2239C3AEAE00097975EE1716F4AD0` without changing the test assertions. The original read/projection body is shared; the invocation owns the moved origin cache, and an `Arc` identity prevents restoration after reader invalidation or into a different reader. A drop guard clears tentative storage, Git, relationship, ancestry and freshness caches on error or unwind.

Actual first GREEN, session 88316 (terminal 0): compilation 11.58 s; **25 query tests passed, 0 failed, 1 diagnostic ignored in 118.47 s**. All five preceding behavioral REDs passed. The same-key stable test now measures exactly two real source captures while retaining zero Git commands and complete output/data preservation. Debug artifact at this stage: `C77CDF8B250125CA0771B13368105093FCEA982B36A69C87F77F6F99F44E0E33`.

Session 26106 (terminal 0): **45 origin-cache tests passed in 60.51 s**, including caps, worker joining, physical evidence and complete legacy-byte checks; **3 SQL snapshot tests passed in 6.86 s**, with one diagnostic ignored; all-target Clippy with warnings denied passed in 8.78 s.

Root then applied the independently reviewed new-API ownership regression patch `83076980343EE0BA231BE376394BC31AF148B2597F15A781BB2ABA5DE4D70DF7`. Session 37624 (terminal 0): compilation 11.23 s, **3 ownership tests passed in 12.50 s**, final all-target Clippy passed in 7.83 s. Controls use genuine active SQL readers and empty receiver caches to isolate foreign/revoked ownership, reject a different linked workspace, verify legitimate round trips, and exercise actual legacy-byte mutation rejection and recovery. Final debug artifact: `6B815D2AEB44839A79952AAECE5CF6447172E21C70B59391DEEF839C47BE313D`.

This is **76 distinct passing unit/regression tests** for this candidate, not a new whole-branch suite or runtime/UI acceptance. Logs use the `target/verification/task6-query-boundary-` prefix: `first-green.log`, `origin-matrix.log`, `snapshot-matrix.log`, `owner-tests.log`, and `final-clippy.log`. Fixed-scale performance comparison and wider integration remain pending at this point. The baseline optimized pipeline test executable was hash-verified and preserved as `release/deps/devmap-5fc8878.exe` (`2E8842BF6BF871E41B4E0CA7E74A9FF5B6D57C93756DAEEB199BD95507FE367C`) before the new optimized build; this does not rebuild or replace the CLI.

### Fixed-scale ABBA diagnostic after consolidation

Optimized library-test build passed in 28.83 s. Candidate SHA-256 is `D76EA5A81D4288A3B077FB1BF92A45E8C9C503D48A1D8F4E8722A83957144971`; the preserved pipeline baseline is `2E8842BF6BF871E41B4E0CA7E74A9FF5B6D57C93756DAEEB199BD95507FE367C`. Sessions 26127 and 52014 both exited 0. Runs under the unchanged artifact parent were `query-stage-L6bUp2` (baseline), `query-stage-ai5UEB` (candidate), `query-stage-PFTn7y` (candidate), and `query-stage-LY18cW` (baseline).

All four reports show root/helper exit 0, an empty owned Job, no abort/errors, and complete post-run preservation. The statistics script also deep-compares all four complete before snapshots: the same 14 SQL tables and immutable backup inventory. Independent review recomputed all means from raw logs, verified executable hashes and inspected the preservation runner.

| Diagnostic stage | Baseline mean ms | Candidate mean ms | Change |
| --- | ---: | ---: | ---: |
| Sealed hot query | 297.4521 | 233.7009 | -21.43% |
| Separate direct application query | 226.2014 | 228.8419 | +1.17% |
| Full legacy inventory/hash | 111.6281 | 110.5323 | -0.98% |
| Separate serial inventory oracle | 283.9011 | 287.8189 | +1.38% |
| Metadata-only diagnostic | 57.7766 | 57.2077 | -0.98% |
| Separate configuration recheck | 31.0360 | 29.0359 | -6.44% |

Each repeated stage has ten observations per variant, five per run, and actual zero Git commands. Hot run means in ABBA order were 292.9550, 236.1162, 231.2856 and 301.9492 ms. Initial query means were 6221.4390 versus 6093.1360 ms, based on only two observations per variant, each with 69 Git commands.

The separate direct application diagnostic still calls the old `project_verified_query` entry point. It does not include the new runtime boundary path and must not be interpreted as a nested component of the new hot query. Neither these independent stage timings nor their changes are additive. The small oracle/application regressions above are recorded, not hidden or attributed without evidence.

The approximately 234 ms diagnostic hot mean **still exceeds 200 ms**. This retained fixed-receipt run uses the diagnostic 60-second cache-routing harness, not the original production two-second freshness/four-client p95 protocol. Cold, freshness, four-client, resource, actual new-runtime UI/host and whole-branch acceptance remain unproven. Statistics and assertions are in `target/verification/task6-query-boundary-statistics.cjs` and `.json`; raw run references and source hashes are retained there.

Wider compatibility verification completed in session 3056 (terminal 0). Compilation took 10.68 s; `application_origin_reanchor` passed 3 tests in 27.46 s, `sqlite_migration` passed 20 in 62.66 s, `sqlite_native_origin_lifecycle` passed 5 in 39.31 s, `sqlite_origin_lifecycle` passed 8 in 91.13 s, and `sqlite_worktree_move` passed 3 in 30.67 s. The separately selected original `dock_model` test for all 256 real workspaces passed exactly one test in 44.89 s after a 10.33 s build; its original refresh budget/assertions were unchanged. Logs: `task6-query-boundary-integration-matrix.log` and `task6-query-boundary-real-256.log`.

The boundary milestone therefore has **116 distinct passing tests** across these focused unit/regression/integration selections. This still does not substitute for the final whole-branch suite, rebuilt CLI/live browser/host verification or original formal performance/resource gates. No installed runtime, user database or parent worktree was changed.

### Complete Rust regression at 4557035

The clean boundary milestone `4557035` subsequently completed `cargo test -- --test-threads=1` in root session 13806, terminal exit 0: **633 passed, 0 failed, 10 explicitly ignored** across 59 Cargo groups, including the empty main/doc-test groups. The lib group contributed 205 passing tests; ignored tests comprise six lib diagnostics, two native migration fixture operations and two explicit performance fixture generators. These ignored operations are not counted as passes. The original 256-real-workspace test passed in the full run.

Raw evidence: `target/verification/task6-4557035-full-rust.log`. The adjacent `task6-4557035-full-rust-summary.json` was derived by `task6-summarize-full-rust.cjs`, selecting the final result within each Cargo group instead of double-counting nested fixture results. Root process termination was checked separately from the log. This command is not a substitute for the plan's final `--all-targets --no-fail-fast` check after all changes, or for live browser, host, performance and resource acceptance.

### Four/eight-worker test-only experiment prerequisites

An independently reviewed test-only patch added four checks to the existing 23 inventory tests. Before implementation, owned run `inventory-faults-IuaNU1` actually produced **24 passed and 3 failed in 58.91 s** (root exit 101, helper exit 71, owned Job empty, no runner errors). Failures were the unmet eight-held-worker/full-queue prerequisites and the unattempted spawn index six. The global quota control passed. Debug artifact SHA-256: `1CAD15F13B152508C954C45AA897B1918DEE3A525B21040336DE1171EDA7E1E7`.

The minimal implementation permits only four or eight through the existing test observation, changing the same core's handle capacity and spawn bound. Non-test builds retain literal four; the queue remains four. Global prefix quotas, complete checked reads, cancellation, join-all, serial fallback and fatal worker panic rules are unchanged. The tests measure actual starts, joins, queue blocking and returned/accepted bytes; exercise full-queue read error and panic, partial spawn refusal, recovery and first/exact/late global quota boundaries; and compare complete manifests with the serial oracle.

Owned GREEN run `inventory-faults-AwpTpx` produced **27 passed, 0 failed in 50.20 s**, root/helper exit 0, owned Job empty and no runner errors. Debug artifact SHA-256: `1878A7E478B295873BD3144CA7E33130359ACD59C5D84BB308CBFC6AA9C87790`. All-target Clippy with warnings denied passed in 8.11 s. Independent source review approved the minimal test-only selection; its stale RED comment was corrected afterward. Logs use `target/verification/task6-eight-worker-` prefixes.

This establishes experiment safety controls, not a measured speedup or authorization to increase production concurrency. The original four-client latency/freshness/resource gates remain open; no eight-worker performance result exists yet.

### Same-binary worker comparison: retain production four

The subsequent diagnostic uses the same core directly with an invocation-local observation, without a global/thread-local selector or a production setting. The existing exact owned-receipt validation selects the unchanged scale fixture. It validates current versus frozen origins and a full serial inventory, then runs one warmup ABBA block followed by five measured ABBA blocks (four, eight, eight, four workers). Formatting and full-manifest comparisons occur outside the timed interval. Existing test-build start/join/hash counters are present equally in both variants; no extra per-read byte instrumentation was introduced. Expected-file counts are not represented as measured physical byte counts.

Actual optimized run `query-stage-Niuv2a`, root session 24827, exited 0. Executable SHA-256: `D95DC2F8FB713A313A0A77D065F99AD1D663FEF2FF3DE9B87569C21FC1D7D993`. All 24 observations completed 500 file hashes, started/joined their selected worker count, reported zero Git commands, zero fallbacks and no errors. The owned Job was empty and not aborted. The wrapper verified complete SQL and immutable-source inventories before/after the run. Build took 30.91 s; subsequent all-target Clippy with warnings denied passed in 9.42 s.

| Workers | Measured samples | Mean ms | Minimum ms | Maximum ms |
| --- | ---: | ---: | ---: | ---: |
| 4 | 10 | 112.3245 | 107.149 | 115.975 |
| 8 | 10 | 111.1946 | 106.743 | 115.459 |

Eight workers improved the observed mean by only **1.01% (1.13 ms)**, with overlapping ranges. This pilot does not establish a reliable production speedup and cannot explain away the roughly 34 ms hot-query gap. Production remains four workers. A complete-query eight-worker rollout is not justified by this result; further work should examine repeated validation and serialized request handling while preserving observation boundaries and resource limits.

Evidence: `target/verification/task6-eight-worker-profile.log`, `task6-eight-worker-statistics.json`, and the referenced run's `profile.log`, `before.json`, `job.json` and `report.json`. The wrapper records its explicit `inventory_workers` mode and checks exact sample order/count and actual process termination. This remains a diagnostic, not the formal four-client p95 or resource gate.

### Current release, four-client scale smoke: gates still fail

The actual `bfa0f11` release CLI (SHA-256 `5C992E17273AD326F8BAE8139191A88B7138F575274A9C3A9E466FEF20220DC3`) ran the existing unchanged smoke protocol against the exact sealed scale receipt in `shared-summary-6IpRxT`. Diagnostic-only commit `c5f7bf2` does not alter its production core. Root session 4133 terminated with exit 0; the report completed without runtime or cleanup errors, while both cold/hot gates explicitly remained false. Smoke success must not be reported as latency acceptance.

Two cold observations were 6975.11 and 7123.12 ms. There were 12 measured requests across three four-client rounds, including eight classified hot requests. Each client's hot population has only two observations: nearest-rank p95 values were **262.93, 985.66, 746.19 and 972.43 ms**. Hot rounds completed in 973.32 and 986.50 ms; the intervening round with full collection took 3969.82 ms. Maximum complete result size was 8781 bytes, below the 32 KiB limit. Read-cohort SQL/immutable preservation checks and cursor audits passed; all nine owned processes were reaped without cleanup errors.

The progression from roughly 262 ms for the first completed hot response to roughly 986 ms for the last, together with the serial executor source, supports investigating repeated work and queueing. It does not prove an unimplemented coalescing optimization will meet the gate. Cold collection also remains independently over budget. Keep the original two-second freshness policy and original formal population of 20 cold observations plus at least 100 hot observations per client; this small run is diagnostic only. Configuration/log: `target/verification/task6-bfa0f11-scale-smoke-config.json` and `task6-bfa0f11-scale-smoke.log`; complete raw evidence is in the referenced run directory.

### Fixed-cohort executor core: prerequisites, not queue activation

The executor's existing per-job operation/seal/encoding sequence was extracted into `runtime/executor/cohort.rs`. The live worker still supplies exactly one job at a time; queue capacity and the two exchange reservations remain unchanged. A bounded core can group consecutive supplied jobs only when their request bytes, complete authenticated source/admin/common/repository identity and full verified connection observations are equal, and the typed operation is Query. Absent/refused proofs, malformed/non-query operations and differing requests stay separate. No result is retained across calls, and no member can join after live validation starts.

Every eligible connection receives its own entry and closing validation. The single original operation runs for the fixed valid group, including its existing source/configuration/SQL/full-hash/projection checks. An operation error retains the original closing-check short circuit and is shared only inside that group. Request buffers are released before bounded response encoding; each response owns its individual reservation, including disconnected receivers. Results are encoded before publication only after closing checks have completed for all valid members.

Actual behavioral RED, root 42598: the original sequential core executed two identical fixed requests twice; all response/reservation controls passed before the final expected 2-versus-1 failure (one failed test, 0.11 s, build 11.20 s). GREEN root 8676 passed the same test (0.11 s, build 10.92 s). Expanded root 48189 passed eight executor tests with zero failures and one explicit isolated-process helper ignored (9.30 s, build 10.92 s). Five new core tests cover shared operation errors with separate reservations, different-query barriers and later independent invocations, missing/refused origins, a disconnected member during a real application projection, and actual source-directory unavailability after a real projection refusing every member. The existing process quarantine, disconnected owner-lock and four serial client tests also passed. All-target Clippy with warnings denied passed in 8.43 s.

Logs: `target/verification/task6-cohort-initial-red.log`, `task6-cohort-initial-green.log`, `task6-cohort-real-projection-matrix.log` and `task6-cohort-clippy.log`. This does not establish four-client admission, queue cutoff/barrier behavior, reservation expansion safety, whole-branch compatibility or a latency improvement. The bounded queue collector, four-client capacity/resource review and actual runtime performance measurement are still required before coalescing can be considered implemented and accepted.

### Bounded collector and four exchange reservations

The candidate worker now collects at most four equivalent queries before calling the fixed-cohort core. Its absolute deadline is set once to one millisecond; parsing or arrivals never extend it. A request observed after the deadline or the first nonmatching request becomes the next pending job. Non-query/refused/unsealed requests incur no batching wait. Queue capacity and the global exchange reservation count are four; logical reservation capacity increases from 48 to 96 MiB. Request/result byte caps are unchanged. This capacity calculation does not establish resident-memory acceptance.

The first prequeued-four behavioral test actually failed with one collected member instead of four (root 39288, one failure in 0.11 s, build 11.34 s). The collector implementation then passed the executor matrix. The deterministic prequeued test uses the same internal collector with a longer test deadline to avoid scheduler-dependent admission; production retains the fixed one-millisecond deadline. A separate already-expired deadline test proves an identical successor remains a fresh observation. A seven-case barrier table covers inventory writes, differing query bytes, repository/admin identity differences, missing/refused proofs and malformed input, retaining pending reservations and leaving later jobs unread.

Final executor matrix: root 16665, **11 passed, 0 failed, 1 isolated-process helper ignored**, 10.19 s after a 10.77 s build. Existing shutdown/quarantine tests now fill the expanded capacity: the owner-lock test also holds two explicit uploading reservations, and the quarantined process test retains all four jobs. They preserve their original no-early-release/no-post-quarantine-execution assertions.

Process verification initially found an obsolete two-reservation expectation: the third partial upload correctly received Ready after capacity became four. The test was updated to occupy all four slots before expecting Busy, retaining its no-write and disconnect/recovery assertions. The original failure is retained in `task6-cohort-process-matrix.log`; that run separately passed all five query-proxy tests in 19.94 s. The corrected root 84830 then passed **seven shared application tests in 42.27 s and seven mutation tests in 79.08 s** (build 10.34 s). The new actual-owner test opens four incomplete uploads, verifies the fifth request gets Busy, releases one connection, verifies that the waiting connection can obtain Ready with a new request ID, and checks unchanged SQL state/integrity. All-target Clippy with warnings denied passed in 8.31 s.

Logs use `target/verification/task6-cohort-collector-` and `task6-cohort-process-` prefixes. These are 30 distinct focused passing tests, not a fresh full suite or a performance/resource pass. Actual batching frequency, cold/hot latency, 600-second CPU/RSS, large-response memory, full browser/host behavior and final branch review remain unproven for this candidate.

### 4dfab2c release smoke after collector integration

Release build at `4dfab2c` completed in 28.10 s. The owned CLI copy, `target/verification/task6-candidate-4dfab2c/devmap.exe`, has SHA-256 `86BB45790F4E9A76E491019B8DEE05B9CEFA9D1CF182F5C5206BD727A9529DC7`. The unchanged scale smoke harness completed in root 20085, exit 0, run `shared-summary-yI3PNu`; there were no request or cleanup errors and read-cohort preservation passed. All 12 measured requests were hot (three per client), with per-client p95 **281.47, 278.83, 279.66 and 280.58 ms**. Four-client rounds took 282.03, 279.38 and 268.57 ms, with completion spreads of only 2.64, 2.35 and 2.26 ms. Complete result size remained 8781 bytes.

The preceding non-coalescing smoke had hot per-client p95 262.93, 985.66, 746.19 and 972.43 ms and roughly 709–724 ms completion spreads. The new smoke supports a substantial reduction in serial queueing, while the first client's p95 is slightly worse. These small, sequentially obtained populations are diagnostic evidence, not a paired statistical study or formal latency acceptance. The new cold observations were 6983.58 and 7321.04 ms. Both hot and cold gates still fail; the 200 ms/3 s limits are unchanged.

Configuration and log: `target/verification/task6-4dfab2c-scale-smoke-config.json` and `task6-4dfab2c-scale-smoke.log`. A separate 600-second acceptance-mode resource observation was then started using the same binary and exact scale receipt. Its result is pending and must not be inferred from the smaller logical queue or successful smoke.

### 4dfab2c default idle lifecycle resource acceptance

That observation completed in root session 32455 with exit 0, run `shared-freshness-1MuAoV`. The same release SHA and exact scale receipt were used with four clients and ten warmups. After closing the proxies, the retained-handle OS observer sampled for 600 seconds at five-second intervals (121 samples), without sending requests to the owner. Owner PID 28716 exited normally with code 0 during the idle phase; the harness did not request its termination. All six owned child processes were reaped, errors and cleanup errors were empty, and final SQL/immutable preservation passed.

The owner remained alive for 60.7737693 seconds of the measured window. CPU consumed during that interval was 0.015625 seconds: **0.02571% of one core** using actual alive time, versus 0.002604% over the whole window. Peak sampled RSS was 24,104,960 bytes; the retained lifetime peak, including startup, was **25,829,376 bytes (24.63 MiB)**. Both CPU and RSS gates passed for the default idle lifecycle. Continuous 600-second residency was not observed and its separate acceptance flag remains false. This owner-only scope excludes proxies, Git descendants and the Python/Node harness; it does not establish aggregate peak memory under maximum-size uploads.

Evidence: `target/verification/task6-4dfab2c-resource-acceptance-config.json`, `task6-4dfab2c-resource-acceptance.log`, and the run's `report.json`/`resources.json`. Default idle resource acceptance is true; overall performance acceptance remains false because cold/hot latency and other gates are still open.

### Current cold-query Trace2 diagnostic

The existing ignored, receipt-checked query diagnostic now records wall-clock boundaries alongside its monotonic durations, permitting exact selection of Git Trace2 events. This is test-only output; production behavior is unchanged. Optimized build completed in 28.49 s and all-target Clippy with warnings denied passed in 7.90 s. Root 84902 completed with exit 0 in `query-stage-JRCtuZ`, using test executable SHA-256 `E356376BD4907F92EA52F60604D05B51493594CACA0E7462E38C2F063E55FDB6` at `892845e` plus the test-clock patch. The owned Job was empty, not aborted, and complete SQL/immutable preservation passed.

The traced initial sealed query took 7503.859 ms and started 69 Git commands; all 69 have matching Trace2 start/exit records within its wall-clock window. The union of those in-Git intervals was 1414.211 ms. That union excludes process creation before Git initializes tracing, supervisor cleanup, filesystem checks and SQL work; it is not total Git-related latency or a subtraction-based attribution of the remainder. Trace instrumentation also makes this a diagnostic rather than a formal latency sample.

There were five each of system/global configuration-path probes and full no-includes configuration listings, three development-target reads, three worktree scans, twenty status reads, and three shared-fact ref/remote-head probe pairs. Some probes are intentional observation boundaries; their repetition alone does not authorize removing checks. Between the first worktree command's trace exit (1371.835 ms) and the next command's trace start (3357.035 ms), roughly 1985 ms is not covered by Git trace intervals. Source inspection places storage/origin/SQL work in this region, but nested timing is still needed to assign its cost. The SQL summary path already streams and verifies full journal records; it must not be replaced with unverified saved heads to meet a budget.

Raw evidence: `target/verification/task6-current-trace-{build,profile,clippy}.log`, and `profile.log`, `git-trace2.ndjson`, `trace-analysis.json`, `before.json`, `job.json`, `report.json` under the referenced run. The analysis script is retained as `target/verification/task6-analyze-current-trace.cjs`. This provides the next investigation targets; no further production speedup is claimed.

### All-target regression after cohort integration

Root session 33263 terminated with exit 0 after `cargo test --all-targets --no-fail-fast -- --test-threads=1`, compiled at `f701e4e`. All **58 Cargo test groups** completed successfully: **646 passed, 0 failed, 10 ignored**. The library contributed 217 passes and six ignored helpers/diagnostics; two native fixture tests and two explicit performance corpus generators account for the other four ignored tests. The test build took 17.60 s. Cargo's `--all-targets` run does not include doctests.

The log parser selects the final result within each Cargo group, avoiding double-counting nested helper results, and the root exit was checked independently. No groups were missing a final result. Evidence: `target/verification/task6-f701e4e-full-rust.log` and `task6-f701e4e-full-rust-summary.json`, produced by `task6-summarize-full-rust.cjs f701e4e`.

Subsequent documentation commits do not change the tested production core. A new test-only SQL component profiling draft was edited after Cargo had compiled this run; it is explicitly outside these compiled test results and requires its own build and execution. These regression results do not close the unresolved cold/hot latency, actual host, live browser pair or whole-objective acceptance gates. Debug-only early returns and ignored diagnostics are not performance evidence.

### Cold SQL input attribution on the sealed scale fixture

The test-only `sql_inputs` diagnostic mode uses the existing exact receipt/identity allowlist and owned Windows Job wrapper. Within one pinned read transaction, each of three cold iterations creates a new summary cache and runs the original `sql_inputs`; its paired warm iteration reuses that cache. Timers cover only the original calls. Outside the timing intervals, the diagnostic checks 100 sessions and 100,000 verified journal records, matching cold/warm summaries, valid route/binding results and unchanged pinned generation. This component deliberately excludes origin enumeration and frozen-file hashing; it does not replace the complete-query verification boundary.

Optimized build root 23306 completed with exit 0 in 33.09 s. Actual run `query-stage-jF3Ryt` then exited 0 with executable SHA-256 `A65926C2C912B75E9A087FFEB8B5F381E15C6130E809875328C5F13446F055B0`. The owned Job was empty and not aborted, all six expected samples were validated, errors were empty, and the wrapper's full SQL/immutable preservation comparison passed. All-target Clippy with warnings denied passed in 8.03 s.

| Iteration | Cold SQL inputs ms | Warm SQL inputs ms |
| --- | ---: | ---: |
| 0 | 1740.252 | 1.877 |
| 1 | 1710.730 | 1.759 |
| 2 | 1667.566 | 2.053 |

The cold component is a substantial startup cost, consistent with the untraced storage-region gap, but these are separate observations and cannot be added to the earlier trace as one timing breakdown. This result does not yet distinguish JSON parsing, canonicalization, hashing and SQL iteration. Any allocation optimization must preserve byte-identical canonical data and every original journal validation. It does not justify trusting unverified saved journal heads.

Evidence: `target/verification/task6-sql-input-profile-{build,clippy}.log`, `task6-sql-input-profile.log`, and the run's `profile.log`, `before.json`, `job.json`, `report.json`. The new helper is covered by this owned diagnostic execution and Clippy; the preceding 646-test result predates its compilation.

### In-place canonical ordering reduces cold SQL verification cost

Inspection of the locked `serde_json 1.0.151` source confirms `Value::sort_all_objects` orders object keys by the same string ordering as the original recursive normalizer. With default sorted maps it does no work; if `preserve_order` is enabled it sorts all nested objects in place. `canonical_json` now uses this method instead of allocating replacement maps/arrays throughout the tree. Typed serialization, recursive floating-point refusal, JSON byte serialization and SHA-256 calculation remain unchanged.

Two additional canonical tests compare 136 representative nested/scalar values against the frozen original normalization algorithm, including Unicode and combining-character keys, escapes, signed/unsigned integer extremes and array order, and check nested floating-point refusal. All five canonical tests passed before and after the change (roots 1643 and 19653, exit 0). The subsequent regression run (root 18090, exit 0) passed **49 distinct tests** across canonical data, event validation, content-addressed storage, legacy journal recovery, SQL journals and the full-verifier comparison/corruption checks. All-target Clippy with warnings denied passed in 8.00 s; formatting and diff checks passed. The earlier 646-test whole suite predates this production optimization.

An optimized candidate build completed in 29.81 s, SHA-256 `51B016C4A1BE15A77DDF1A37C508C53DF425D1C35BE4E37FEC3216C3FEA32E2B`. The preserved A659 baseline and candidate ran sequentially in ABBA order against the unchanged sealed scale receipt, using the same diagnostic mode. Root 69035 exited 0. Runs `query-stage-hCxTnt`, `query-stage-uBgKoA`, `query-stage-Qu6ScE`, `query-stage-BjRJxi` all completed with empty owned Jobs, no errors and full SQL/immutable preservation. Every cold iteration still verified the existing 100,000 records; warm-cache summary parity also passed.

| Variant | Cold samples | Mean ms | Minimum ms | Maximum ms |
| --- | ---: | ---: | ---: | ---: |
| Original normalization | 6 | 1575.226 | 1551.811 | 1600.609 |
| In-place ordering | 6 | 1380.133 | 1362.136 | 1394.257 |

The observed reduction is **12.39% (195.09 ms)** for this component. This small diagnostic population does not establish full cold-open p95 or hot-query acceptance. No complete release CLI latency rerun is included here, and the 3-second/200-ms gates remain open. Raw logs use `target/verification/task6-canonical-` prefixes; `task6-canonical-abba-statistics.json` records the aggregate calculation. Original and candidate executable hashes distinguish this result from the earlier unpaired cold component observations.

### Single-allocation digest encoding

`sha256_hex` now encodes the unchanged SHA-256 digest into one 64-character string using a fixed lowercase alphabet, instead of allocating a formatted string for each byte. A new test compares all output bytes with the original formatter for 261 binary inputs, including block boundaries, and checks the standard empty-input vector; the existing `abc` vector and canonical/content-ID tests remain. Six canonical tests passed. Optimized build completed in 29.47 s, executable SHA-256 `4CAB11E3DBE44B0E6839D0B1A31395475B4F33A82C21552AA4CB2EB192490E44`.

ABBA root 72378 exited 0 using the preserved 51B canonical-ordering baseline and this candidate. Runs `query-stage-gbihU0`, `query-stage-lZ9OKA`, `query-stage-6ZOtbM`, `query-stage-3sKkD0` all verified the original 100,000 records, matching warm summaries, empty owned Jobs and complete SQL/immutable preservation without errors. Six cold samples per variant gave mean **1328.375 ms before / 1232.963 ms after**, a **7.18% (95.41 ms)** reduction. Baseline range was 1311.447–1337.609 ms; candidate range was 1218.055–1273.638 ms. These populations are separate from the preceding experiment; their percentages should not be added as one measured overall speedup.

Root 27596 exited 0 after **41 distinct focused tests**: five streaming-summary verification/corruption tests, six canonical tests, four content-addressed repository tests, thirteen legacy journal tests and thirteen SQL journal tests. All-target Clippy with warnings denied passed in 8.12 s; formatting/diff checks passed. Logs use `target/verification/task6-hex-` prefixes and aggregate values are in `task6-hex-statistics.json`. Whole-branch tests and actual release CLI/browser/host/performance acceptance remain to be refreshed after these production changes; this component result does not close those gates.

### e4bc631 actual release smoke: both latency gates remain open

The actual CLI rebuilt in 25.94 s at `e4bc631` (root 60892, exit 0). Its verified copy is `target/verification/task6-candidate-e4bc631/devmap.exe`, SHA-256 `60B43BAA9251FD618CE0CF8AC4697E3EA66A101AECEEDC9B897452505D48C8C4`. Only candidate path, revision and hash changed in the existing scale smoke configuration. Root 79415 exited 0, run `shared-summary-m34fMu`; no request/cleanup errors occurred, all nine owned children were reaped and read-cohort preservation passed.

Two cold observations were **6959.5414 and 6680.0917 ms**. All twelve measured requests were hot, three per client; per-client p95 was **258.7618, 257.8929, 256.7454 and 259.1534 ms**. Four-client rounds took 256.0878, 254.3800 and 259.7044 ms, with completion spreads below 2.41 ms. Maximum complete response remained 8781 bytes; cursor audits passed. The hot/cold gate flags and overall acceptance remain false.

The preceding 4dfab2c smoke had per-client p95 around 279–281 ms and cold observations 6983.5754/7321.0400 ms. The new small, unpaired observations are consistent with some improvement but do not establish a formal speedup or acceptance. Full cold request processing still takes 6111–6382 ms, beyond owner startup and initialization; reducing SQL allocation alone is insufficient. Keep the original 200 ms/3 s gates and formal populations. Further investigation should address repeated configuration/Git observation costs without weakening source proofs. Evidence: `target/verification/task6-e4bc631-{release-build,scale-smoke}.log`, `task6-e4bc631-scale-smoke-config.json`, and the referenced run report/raw rows.

### Current Git supervisor attribution

The existing ignored `profile_ten_sequential_git_versions` diagnostic ran from an owned non-repository directory using the preserved 4CAB optimized test executable and Windows Job wrapper. An initial wrapper parse missed the first JSON record because Rust's test-name prefix shared its line; the test itself exited 0 with an empty Job. That failed report remains in `git-supervisor-2Zosp7`. The parser was corrected to strip only the exact known test prefix, retaining the expected 120 records and ten occurrences of every stage.

The corrected run `git-supervisor-y4LZZx` exited 0 with no errors and an empty, non-aborted Job. For ten sequential `git --version` calls, mean outer duration was **62.501 ms**. Mean stages were: suspended spawn 4.915 ms; tree attachment/resume 0.137 ms; root wait with concurrent pipe reads 35.796 ms; confirmed cleanup 21.417 ms; reactor build 0.024 ms and drop 0.174 ms. The total/outer measurements enclose these stages and must not be added to them.

This isolates supervisor behavior, not repository-query timing. It supports prioritizing the number and serial arrangement of Git invocations over reactor reuse, whose measured setup/teardown cost is small. The cleanup interval is required evidence that owned children have stopped; this result does not justify bypassing cleanup, changing Git executable selection or detaching readers. Configuration-path probes remain serial in both `QueryConfiguration::capture` and shared relationship evidence capture; any overlap must preserve bounded admission, the original observation boundaries, absolute operation deadlines and joined failure handling. No production change is included in this diagnostic.

Evidence: `target/verification/task6-profile-current-supervisor.cjs`, `task6-current-supervisor.log`, `task6-current-supervisor-corrected.log`, and the two run directories' logs/reports.

### Overlap independent configuration-path discovery

Both query configuration acquisition and shared relationship evidence capture now overlap the independent `git var GIT_CONFIG_SYSTEM` and `git var GIT_CONFIG_GLOBAL` probes. One scoped worker discovers global paths while the caller discovers system paths. Physical eligibility checks still precede the pair; candidate files are still witnessed before the full configuration listing. Every capture/recheck boundary and the global four-process admission limit remain intact.

The worker inherits the caller's absolute Git budget, including an already expired deadline. A failed worker spawn retains the original serial short circuit. Started workers are joined on ordinary failure or unwinding. Concurrent discovery may attempt the global probe even when the system probe declines; either Git supervisor failure is fatal rather than optional-proof fallback. System errors win fatal ties and ordinary error ordering remains system-first.

Six new tests cover real expired-budget Git refusal, deterministic overlap and exact deadline inheritance, fatal/ordinary error precedence, spawn-refusal fallback, and caller/worker panic cleanup. The shared-facts suite passed 25 tests in 48.88 s; query-validation regression passed 25 tests with its explicit diagnostic ignored in 117.75 s. Both root processes exited 0. All-target Clippy with warnings denied passed in 8.41 s; formatting and diff checks passed. Logs: `target/verification/task6-config-paths-{tests,query-tests,clippy}.log`. Full release timing and whole-branch acceptance remain separate gates.

The actual release CLI at `1656f9a` built in 26.92 s, SHA-256 `02218C89570DD88744D3144E9E26DFA3B534974EB634271582B0E1A4506FCBEA`. The owned C-drive copy was checked against its D-drive source. Smoke root 80950 exited 0, run `shared-summary-Yw1yi7`, with the same scale receipt and unchanged harness settings (only candidate path/revision/hash differ from the e4bc631 configuration). All nine owned children were reaped; errors and cleanup errors were empty. SQL and immutable corpus preservation, complete paging and Unicode checks passed.

Two cold opens were **6407.623 / 6352.151 ms**; request portions were **5835.212 / 5836.409 ms**, with owner-ready 284.726 / 223.326 ms and initialization 260.600 / 268.000 ms. Four clients supplied three measured hot observations each: per-client p95 **251.814 / 253.565 / 252.465 / 252.896 ms**. Maximum complete summary was 8781 bytes. Both latency gates and overall performance acceptance remain **false**. The small unpaired cold observation is below the previous 6680-6960 ms range, but is not a formal performance comparison or the required 20-cold/100-hot-per-client acceptance population. This overlap does not change the no-Git hot path. Evidence: `target/verification/task6-config-paths-build.log`, `task6-1656f9a-scale-smoke{,-config.json}` (log extension `.log`), and the run's report and raw observations. Current whole-suite/browser/host/resource acceptance is still outstanding.

### Reuse the witnessed development-target configuration

QueryConfiguration now obtains the last admitted devmap.developmenttarget value from the already witnessed, scope-ordered Git configuration listing. It removes the subsequent git config --get invocation. The original physical witnesses and closing recheck remain. Empty strings and spaces are retained; valueless entries, control characters and includes still decline to the original projection path.

The isolated real-Git differential test covers eight eligible configurations: absent, empty, ordinary, duplicate, global-only, mixed-case local override, Unicode with surrounding spaces, and escaped quote/backslash. Three unsupported configurations (valueless, embedded newline and includes) still decline. Expected values come from the original GitRelationshipResolver::development_configuration, not a second parser. The test also requires three actual Git invocations per eligible acquisition. Before the production edit it failed specifically at four versus three invocations (root 98968, exit 101); after the edit it passed in 3.34 s (root 5284, exit 0).

The query-validation suite passed 25 tests with one explicit diagnostic ignored in 114.09 s (root 30247, exit 0), including configuration drift and physical source replacement. All-target Clippy with warnings denied passed in 8.30 s; formatting and diff checks passed. Evidence: target/verification/task6-config-listing-{red,green,query-tests,clippy}.log. This removes redundant discovery but does not replace journal verification or prove the full latency gate.

Actual release CLI 4611246 built in 26.89 s, SHA-256 F5C623B07401E70C0AE61E10CF363F908CCFBD01A43F5494DF97FAE4B7313A65; its owned C-drive copy matched the build output. Smoke root 94193 exited 0, run shared-summary-kNI0Hy, with the unchanged sealed scale fixture and settings. Two cold opens were 6415.462 / 6270.530 ms (request portions 5868.960 / 5738.910 ms). Per-client hot p95 over three observations each was 251.318 / 249.944 / 251.783 / 250.716 ms. Complete response maximum remained 8781 bytes. All nine owned children were reaped, errors and cleanup errors were empty, and SQL/immutable preservation passed. Both latency gates remain false. These small unpaired measurements overlap the prior candidate and do not establish a significant end-to-end improvement. Raw evidence: target/verification/task6-config-listing-build.log, task6-4611246-scale-smoke.log, task6-4611246-scale-smoke-config.json and the run report/observations.

### Current-stage attribution after configuration reuse

Optimized library test build completed in 28.78 s, SHA-256 29A97B8388A2CF0BEEE34620636F4ABC138E218B77DB1E088B5265BB2C970E47 (distinct from the actual CLI). The first diagnostic attempt explicitly set query_stages, which the Rust diagnostic rejects; run query-stage-dtO4JH exited 101 inside an empty owned Job and preserved all data. The corrected invocation left that optional mode unset as required. Root 92033 exited 0, run query-stage-z7lKB1, with no errors, an empty non-aborted Job and complete SQL/immutable preservation.

Current cold trace contained exactly 67 Git starts, including one development-target get instead of the previous three. Its sealed query took 6615.997 ms; the union of Git Trace2 start-to-exit intervals was 1238.820 ms. This is instrumented diagnostic time, not actual CLI acceptance, and the remaining time also includes supervisor setup/cleanup, filesystem and SQL work. It cannot all be assigned to one non-Git component.

Five diagnostic iterations averaged 232.566 ms for sealed hot queries and 222.409 ms for application projection. Independent component observations included complete legacy inventory hashing 107.207 ms, origin-proof recheck 45.762 ms, configuration source-resolution capture 28.993 ms, and standalone serial manifest checked-open/link-count work 157.253 ms. These observations overlap or use independent paths and must not be summed into a fabricated hot-query breakdown. They justify investigating complete-file validation and physical proof costs next; the eight-worker comparison already showed no useful improvement, and removing integrity checks remains outside scope. Logs: target/verification/task6-config-listing-profile-build.log, task6-4611246-query-profile{,-corrected}.log, task6-4611246-trace-analysis.log; run profile.log, git-trace2.ndjson, trace-analysis.json and report.json.

### Capture-local source directory identity sandwich

SourceResolutionWitness now records each distinct ancestor identity once during acquisition and reopens all recorded directories after all source file reads. Every file path still passes the original checked metadata and canonical-directory traversal; each file still uses the original checked open, full bounded byte read and replacement re-open. Directory identities are never replaced by a later observation in the same capture. This removes repeated identity opens for shared ancestors while adding an explicit closing identity comparison. Each new capture starts with empty evidence; no cross-query identity cache or TTL is introduced. General configuration/shared-relationship directory capture retains its previous implementation.

Three tests passed in 0.16 s: complete evidence equality with the original file/ancestor capture over 24 references plus a missing path, rejection after directory removal, and rejection when a replaced ancestor would otherwise overwrite its earlier identity despite identical file bytes. Query-validation regression passed 25 tests with one diagnostic ignored in 115.61 s (root 95295, exit 0). All-target Clippy with warnings denied passed in 8.55 s; formatting and diff checks passed. Evidence: target/verification/task6-source-directory-{tests,query-tests,clippy}.log. Performance measurement remains pending for this candidate.

ABBA root 27943 exited 0 using the preserved 29A97B library baseline and new A76A0FD44410919974E74A1A39E0C1A242A0BAA5C1C6589EF646E9C6754B6924 candidate (optimized build 31.96 s). Runs query-stage-XF6nKo, query-stage-ZFDSBS, query-stage-WG76Or and query-stage-JlmEE6 all had empty non-aborted owned Jobs, zero errors and full SQL/immutable preservation. Each variant supplied ten warm diagnostic observations and two cold observations; Trace2 was disabled for both.

Source-resolution component mean decreased from 29.025 to 12.317 ms; complete configuration recheck from 32.972 to 16.285 ms. The independently measured origin-proof recheck decreased from 44.989 to 38.137 ms. Sealed hot-query mean decreased from 227.397 to 217.717 ms (4.26%, 9.680 ms); ranges were 222.738-236.268 and 211.419-226.471 ms. Cold diagnostic means were 5363.586 and 5166.112 ms. These are small diagnostic populations with a controlled hot window, not actual concurrent CLI p95 or the formal acceptance population. Overlapping component timings must not be added together. Evidence: target/verification/task6-source-directory-abba-{0,1,2,3}.log, task6-source-directory-abba-statistics.json and each run's profile/report/Job records. The new terminal parent-identity check and complete file validation were present throughout the candidate measurement.

Actual release CLI 1c03505 built in 27.49 s, SHA-256 940C50692C647E21C7C389BCCF270CEC895BB63C584020903FAA185087F7531A. Its owned C-drive copy matched the D-drive build. Root 51197 exited 0, smoke run shared-summary-XZmOYJ, using unchanged scale settings. Cold opens were 6270.213 / 6254.521 ms, request portions 5693.117 / 5682.182 ms. Per-client hot p95 (three observations each) was 245.040 / 245.182 / 245.631 / 244.558 ms. Response maximum was 8781 bytes; all nine children exited, no spawn/cleanup/query errors occurred, and full preservation passed. Latency and overall performance acceptance remain false. Evidence: target/verification/task6-source-directory-cli-build.log, task6-1c03505-scale-smoke.log, task6-1c03505-scale-smoke-config.json, and the run report/raw observations. Formal population, current whole-suite, browser pair, host and resource gates are still open.

### Whole-branch regression refreshed after the measured optimizations

All-target Rust testing at source 1975f57 / production 1c03505 completed with root 51559 exit 0: 659 passed, zero failed and ten intentionally ignored fixtures/diagnostics across 58 groups. This supersedes the stale 646-test source-level regression as evidence for the current canonical encoding, configuration probe, witnessed target and source-directory changes. It does not supersede any missing release performance, browser, host or resource gate. The actual current CLI smoke remains cold approximately 6.25 s and per-client hot p95 approximately 245 ms; both target thresholds remain unmet. Full details and raw evidence are recorded in the compatibility audit and task6-1975f57-full-rust log/summary.

### Bounded record parsing diagnostic, not a production verifier

An explicitly selected test-only `record_parse_batches` mode first runs the existing complete SQL-input validation/parity helper, then compares serial/four-worker/four-worker/serial parsing of the same 100,000 owned fixture records. All passes use one pinned SQLite read transaction. Each batch is limited to 512 records and 4 MiB, plus one bounded look-ahead record. Workers use the existing canonical-record parser and return hashes in row order; the diagnostic checks record count, ordered aggregate digest and unchanged store generation. This experiment does not replace sequence, index, duplicate-ID, chain or accepted-head validation and is not a production journal verifier. Batch worker spawn refusal and production admission policy remain implementation work.

Optimized library build succeeded in 76 s; binary SHA-256 was FE2FE41E4A84CE6082BCB2ACDE970E025559DB2DFF6662D2119425671C1B50F2. Owned diagnostic root 51621 exited 0, run `query-stage-ePnyDi`; its child Job was empty, not aborted, with root exit 0. Full before/after SQL and immutable-file preservation passed with no errors. Serial passes took 1093.257 and 1073.356 ms; four-worker passes took 572.550 and 595.104 ms. Mean parsing time decreased 46.11%, from 1083.307 to 583.827 ms. All four ordered digests were 787e9c87b69c0fda223eb1e538c403a066a8e1511c39478db7d077597b6b452e. Each pass used 196 batches with peak raw input 444526 bytes; each parallel pass joined 784 workers. This input counter excludes parsed allocations and is not an RSS measurement. Timing includes SQL row reads and thread creation but excludes the preliminary complete verifier.

All-target Clippy with warnings denied passed in 11.89 s (root 27565 exit 0). Production remains 1c03505; the previous 659-test result does not cover this new diagnostic. The four-pass component observation is promising but is neither end-to-end improvement nor formal latency acceptance. Next implementation must retain the single transaction, ordered full validation, bounded memory and worker admission, error precedence, spawn-failure fallback and worker cleanup. Evidence: `target/verification/task6-record-parse-profile.log` and the run's profile, report, preservation and Job files.

### Production bounded journal parsing with ordered verification

The SQL summary reader now collects at most 512 owned rows per batch, stopping after 512 KiB of raw JSON plus indexed IDs. Both strings retain the SQL-side MAX_RECORD_BYTES cap, so one threshold-crossing row adds at most twice that cap. Payload parsing keeps at most four active records and retains only compact sequence/identity/hash results for ordered validation; these logical bounds are not an RSS measurement. The original caller-owned SQLite transaction remains the only database reader. No extra connection, persistent worker pool, cross-query cache or integrity shortcut was added.

A process-wide nonblocking permit allows only one batch to start up to three helper threads; the caller parses the fourth part. Small batches and busy admission use the caller. Failed thread creation falls back to parsing that part on the caller. Scoped threads finish before admission is released, including worker or caller unwinding. Row decoding errors are deferred behind earlier rows; size/resource checks, canonical parsing errors, identity/sequence/duplicate/link checks and final accepted extent remain ordered as before. Parsing may speculatively inspect later bounded rows, but those errors cannot replace an earlier validation error.

Journal tests passed 11/11 in 1.29 s, including new 63/64/511/512/513/1025-record boundaries, earlier identity failure before later SQL decode failure, metadata-before-parser error precedence, absolute parse line offsets, ordered four-thread work, busy-admission fallback, injected spawn refusal, and caller/worker panic joining/admission release. Snapshot tests passed 3 with one explicit diagnostic ignored in 6.56 s, covering same-generation external tamper invalidation, activation during legacy loading and one-snapshot generation/domain consistency. All-target Clippy with warnings denied passed in 12.07 s; formatting and diff checks passed. Whole-suite and actual CLI/browser/resource evidence must be refreshed for this production change.

Optimized library SHA-256: 8F461A8A376A347B61F2C2E3F43F2804892B4AD6A567393377047DF3A504A186; build root 65933 exited 0 after 75 s. The first SQL-input ABBA (root 11940) overlapped Clippy and is retained as non-decisive evidence. A second ABBA ran after both compilation and tests finished: root 71742 exited 0, runs query-stage-XNkfQC / query-stage-z75lLK / query-stage-YCQOHV / query-stage-IVTYNX. All four owned Jobs were empty, non-aborted and successful, with zero errors and complete SQL/immutable preservation. Each variant supplied six cold SQL-input observations; mean decreased from 1148.972 to 579.979 ms (49.52%). Baseline range 1134.611-1159.248 ms; candidate 553.009-603.878 ms. Full SQL-input parity and expected record counts were asserted outside timing in each run. This measures the complete SQL-input component, not end-to-end cold start or formal concurrent latency. The actual release CLI is still the previous 1c03505 build and its last performance result remains failing.

Evidence: target/verification/task6-production-batch-{clean-abba,abba}-{0,1,2,3}.log and each run's report/profile/preservation/Job files. Debug journal root 33464, snapshot test root and Clippy root 80548 all exited 0. Next: rebuild actual CLI, repeat owned scale smoke, then refresh broader correctness and acceptance gates. Do not mark goal complete from this component improvement.

Actual CLI 8907988 rebuilt successfully in 62 s (root 76353 exit 0), SHA-256 70334D3DF745F2D9BD5A63BE675011B575DD4C6D433964C6EA803A611857BCB8. Its new owned C-drive copy matched the D-drive build. Smoke root 34231 exited 0, run shared-summary-IQ9OKn, with only candidate path/hash/revision changed from the previous scale configuration. Cold samples were 5490.344 and 5435.582 ms; request portions 4970.061 and 4920.063 ms, owner-ready portions 233.387 and 222.543 ms, initialization 260.902 and 268.828 ms. Four-client hot p95 values were 232.157 / 232.750 / 233.222 / 233.649 ms, three measured observations per client. Maximum response was 8781 bytes. All nine owned children exited, spawn/query/cleanup errors were empty, and preservation_read_cohorts passed. Both latency gates and overall performance acceptance remain false. This is smoke scope, not the formal 20-cold/100-hot-per-client population.

Compared with the previous smoke, cold time improved from about 6.25 s to about 5.46 s. This is a sequential smoke comparison on an uncontrolled filesystem cache, not an isolated causal estimate for hot-query changes. Current returned observation timestamps place completed Git collection roughly 2.85-2.88 s after storage-input observation; this interval includes work around collection and is not a precise phase timing. Source inspection confirms GitRelationshipResolver already parallelizes independent status requests under the existing Git process budget, so merely adding another status-worker layer is not the next fix. Next diagnostic should distinguish topology, relationship shared-fact acquisition and closing collection proofs inside DockProjectionContext::collect, while retaining its before/after worktree/topology checks. No production code changed during this CLI measurement turn. Evidence: target/verification/task6-8907988-scale-smoke-config.json, task6-8907988-scale-smoke.log and run report/raw observations.

### First-collection and relationship phase diagnostic

Test-only phase clocks now instrument the existing DockProjectionContext collection and relationship resolver paths under the explicit owned-fixture `collection_phases` mode. Production builds contain none of the clocks. Three consecutive collections use no route targets and assert exact equality of worktree descriptors, topology, relationship report and effective configuration key, plus expected worktree/relationship counts. Original opening/closing worktree and topology checks remain in place. The mode reuses the exact receipt/identity allowlist and external SQL/immutable preservation wrapper; it is a component diagnostic, not a summary acceptance test.

Initial collection-only run query-stage-SJ2ki3 (root 71937 exit 0) located relationships at 2115-2174 ms out of 2854-2918 ms total. The expanded diagnostic then passed in query-stage-cFzgUt (root 18628 exit 0), optimized library SHA-256 C4B9225CC385EB2CA674A4A2B9E411E9C82FB719856F27208A8AD4CCEFD0AAA7. All three collections matched. Totals were 2872.677 / 2819.041 / 2830.500 ms. Relationships took 2134.228 / 2089.490 / 2103.760 ms (45 Git starts each). Nested initial parallel probes took 484.166 / 463.379 / 466.613 ms (24 starts), target selection 62.200 / 60.364 / 61.457 ms (one start), shared facts 1581.896 / 1564.471 / 1574.383 ms (20 starts), and final row resolution approximately 1 ms (zero starts). Topology itself remained approximately 248 ms (four starts). Nested clocks overlap the outer relationship timing and must not be added to it.

The shared-facts phase is now the justified next target. Its acquire path captures evidence twice, then rechecks after representative facts; each capture revisits directory ancestry and performs configuration/ref discovery. Existing query-source resolution already has a separately tested capture-local first-identity/closing-check optimization, while the general shared-facts directory helper still reopens and overwrites repeated ancestor entries. Reusing that pattern requires preservation tests and measurement, not simply removing repeated validation. The current diagnostic does not isolate filesystem time within shared facts, so no precise saving is claimed yet.

Expanded optimized build root 20022 exited 0 in 73 s; all-target Clippy root 42061 exited 0 in 11.65 s. Initial build 27629 and Clippy 3707 also exited 0. Both owned runs had empty non-aborted Jobs, root exit 0, zero errors and complete SQL/immutable preservation. Actual CLI remains production 8907988 SHA70334D3D..., with cold/hot acceptance still false. Evidence: target/verification/task6-collection-phases.log, task6-relationship-phases.log and the two run directories' reports/profile/Job/preservation files.

### Shared-facts capture-local directory evidence

The shared relationship proof now uses the existing first-directory-identity capture helper and closes every distinct ancestor at the end of each complete capture. Its two acquisition captures and later recheck remain separate. All path metadata/canonical traversal, bounded file reads, file replacement reopens, Git configuration/ref probes and evidence comparisons remain present. Pointer and absent-file helpers now accept the capture's directory strategy; general query-configuration capture retains its previous strategy. No directory identity is cached across captures. The original directory reader remains available for an independent complete-evidence oracle test.

Thirty shared-facts tests passed in 52.20 s (root 47785 exit 0), including exact original/candidate evidence on a real linked-worktree fixture, removed/replaced-directory closing rejection, configuration/target/tag drift, Git deadline handling and complete original fallback reports. All-target Clippy passed in 11.69 s; optimized library built in 77 s (combined root 58857 exit 0). Formatting and diff checks passed.

Sequential ABBA root 18929 exited 0 with no concurrent build/test workload: baseline C4B9225CC385EB2CA674A4A2B9E411E9C82FB719856F27208A8AD4CCEFD0AAA7, candidate C993EA97FF1EF3819A27172ECE9A384537F674396C2B577B333C647C70053A98. Runs query-stage-fWHAER / query-stage-wpBe3k / query-stage-6BqmpK / query-stage-RjUUgw all had empty non-aborted Jobs, successful root exits, no errors and complete SQL/immutable preservation. Each variant provided six collections with exact output equality within each run. Shared-facts mean decreased from 1583.650 to 1260.196 ms; complete collection from 2848.635 to 2525.322 ms. Collection ranges were 2809.617-2902.433 versus 2507.018-2572.574 ms. Relationship mean decreased from 2110.583 to 1790.197 ms. Nested values overlap and must not be added. These are collection component observations with no route targets, not end-to-end or formal latency acceptance.

Actual CLI remains the previous production 8907988 build; no end-to-end saving is claimed for this new source yet. The next remaining shared-facts cost includes the unchanged Git discovery probes and representative commands. Further reduction must preserve probe error precedence, source-evidence boundaries, original fallback and the closing proof. Evidence: target/verification/task6-shared-directory-abba-{0,1,2,3}.log, task6-shared-directory-abba-statistics.json and the four run report/profile/Job/preservation records.

### Current CLI smoke after shared-directory optimization

Actual release CLI 211cae3 built in 63 s (root 25601 exit 0), SHA-256 A94ADB01A0D83DC397D04EA94D0A4892E63C4C60878EFE2B38AFC6A8829CCB10; its owned C-drive copy matched the D build. Smoke root 16175 exited 0, run shared-summary-iIPeSq. Only executable path, hash and source revision changed from the previous scale configuration. Cold samples were 5186.858 / 5188.419 ms; request portions 4657.901 / 4672.344 ms. Four-client hot p95 values were 239.185 / 239.905 / 240.503 / 241.041 ms, three observations each. Maximum response remained 8781 bytes. All nine owned children exited, no spawn/query/cleanup errors occurred, and complete preservation_read_cohorts passed. Both latency gates remain false. This is smoke, not formal acceptance; the sequential cold improvement from approximately 5.46 s does not establish a hot-query improvement (the current small hot sample was slower).

Evidence: target/verification/task6-211cae3-scale-smoke-config.json, task6-211cae3-scale-smoke.log and the shared-summary-iIPeSq report/raw observations. A full all-target Rust regression was started only after smoke completed to avoid benchmark contention. Its library group at source 211cae3 passed 233 tests with zero failures and six ignored in 331.79 s. This is library-only evidence: the same owning process is proceeding through integration groups, and no whole-suite result is asserted here.

Full current all-target Rust regression at production 211cae3 has now completed: root 16066 exit 0, 665 passed / zero failed / ten ignored across 58 complete successful groups. This supersedes the earlier library-only status. It is correctness evidence for the current measured optimizations, not a performance acceptance result; current actual CLI remains cold about 5.19 s and per-client hot p95 239-241 ms. Native old-binary migration/browser, formal performance, current host/resource and final acceptance checks remain open. See the compatibility audit and task6-211cae3-full-rust log/summary for scope and ignored-case details.

### Overlap independent shared-proof Git probes

After all candidate configuration files have been witnessed, shared-facts capture now overlaps configuration, refs and remote-HEAD reads using two nested scoped pairs. Configuration validation is consumed before ref validation, followed by remote-HEAD validation and the original closing directory proof. The original two acquisition captures and later recheck remain intact. Workers inherit the caller's absolute Git budget; the global four-process limit is unchanged. Started workers are joined even on ordinary errors or unwinding. Any started supervisor error remains fatal instead of becoming an optional-proof decline; primary errors win fatal ties. A failed worker spawn uses the corresponding serial primary-first short circuit. No persistent worker pool, extra cache or removed Git command is introduced.

Five new tests verify three-probe overlap and exact deadline inheritance, started fatal errors at every nested position and ordinary-error precedence, spawn-refusal short circuit, caller/worker panic joining, and real Git refusal on an expired budget. The complete shared-facts set passed 35 tests in 46.34 s (root 66818 exit 0). All-target Clippy passed in 11.69 s and optimized library build in 76 s (combined root 54791 exit 0). Formatting/diff checks passed.

Sequential ABBA root 5472 exited 0: baseline C993EA97FF1EF3819A27172ECE9A384537F674396C2B577B333C647C70053A98, candidate 2FDD58BF5F707845DC96756E0191A007681EF51F4C1185C3FF8F2CA178207B24. Runs query-stage-RgCu4i / query-stage-Sii8T9 / query-stage-pQq6w1 / query-stage-id1aKb all had empty non-aborted Jobs, successful exits, zero errors and complete SQL/immutable preservation. Six observations per variant: complete collection mean 2556.985 -> 2237.881 ms; ranges 2511.688-2601.821 vs 2191.522-2273.391 ms. Shared-facts mean 1272.917 -> 952.972 ms; relationship mean 1805.441 -> 1496.784 ms. Nested phase durations overlap and must not be added. Each diagnostic run asserted exact complete collection output equality on the owned 20-worktree fixture, without route targets.

This is component evidence, not actual CLI latency acceptance. Actual CLI, full 665-test regression, fresh native migration/browser and owner-restart evidence still belong to production 211cae3 and must not be relabeled as this new source. Evidence: target/verification/task6-probe-overlap-abba-{0,1,2,3}.log, task6-probe-overlap-abba-statistics.json and run reports/profile/Job/preservation records.

### Actual CLI and hot-path refresh at 45b1215

Actual release CLI built in 63 s (root 63084 exit 0), SHA-256 BFB69906162BCCF7C2A3E5C66E8A315C8B35FD538F07673921BB196F1E0708C7; the new owned C-drive copy matched the D build. Smoke root 20416 exited 0, run shared-summary-j0HHGm, with only candidate path/hash/revision changed. Cold samples 4883.947 / 4951.337 ms, request portions 4356.063 / 4361.296 ms. Per-client hot p95 239.428 / 239.941 / 238.741 / 237.866 ms, three observations per client. Response maximum 8781 bytes; all nine children exited, zero spawn/query/cleanup errors, complete preservation true. Both latency gates remain false. Sequential smoke indicates cold improvement but no material hot improvement; formal population was not run.

The untraced owned query-stage diagnostic was refreshed with optimized library 2FDD58BF5F707845DC96756E0191A007681EF51F4C1185C3FF8F2CA178207B24. An initial invocation accidentally supplied an empty mode through PowerShell environment handling and was explicitly rejected (run query-stage-KL9bZM, child exit 101 / wrapper 71, empty non-aborted Job, preservation true). Removing the mode variable through the environment provider corrected the invocation; root 55555 exited 0, run query-stage-tNrThq, empty non-aborted Job, no errors, full SQL/immutable preservation. Do not set an empty mode or explicitly set query_stages; the Rust diagnostic currently selects that default only when the variable is absent.

Five controlled-hot-window diagnostic queries averaged 209.170 ms with zero Git starts; verified application projection averaged 203.221 ms. Independent full legacy inventory hashing averaged 104.589 ms, origin-proof recheck 37.343 ms, configuration recheck 15.821 ms, source configuration resolution 12.440 ms. Independent serial manifest-file open/link-count work averaged 154.992 ms, stream reads 17.539 ms and hashing 42.582 ms; these serial component measurements are not the production parallel path. The separately measured serial full inventory averaged 273.957 ms. Do not sum overlapping independent components or equate five in-process hot observations with concurrent CLI p95. Initial sealed query took 3821.808 ms with 67 Git starts; this omits setup performed before the diagnostic timer and is not CLI cold-open time.

The next hot-path investigation should target complete immutable-file/origin validation costs rather than more cold-only Git probe overlap. Full bytes, file identities/link counts, physical source checks and closing observation boundaries remain required. Actual legacy hashing already uses a 64 KiB stream buffer; the 8 KiB origin-proof admission buffer is a different reader and must not be mistaken for the 86 MB legacy corpus reader. Evidence: target/verification/task6-45b1215-scale-smoke-config.json, task6-45b1215-scale-smoke.log, task6-45b1215-query-profile{,-corrected}.log, task6-45b1215-query-stage-statistics.json and each run's report/raw/Job/preservation files.

### Serial manifest open decomposition (test-only)

The existing independent serial file-I/O diagnostic now separates parent canonical traversal, checked payload open/identity validation, and hard-link-count validation. All original calls and full byte/hash comparisons remain in their original order; production code is unchanged. Nested times overlap the existing checked-open total and must not be summed with it or interpreted as parallel production-query savings.

Optimized library build root 81808 exited 0 in 75 s. Owned diagnostic root 50906 exited 0, run query-stage-8AweKU, executable SHA-256 E3629253421EB904F197D074BEB815EA7D4AF0202E70B9C1A8F4A89FCF3FED83 (505274b plus diagnostic changes). The diagnostic passed 1 test in 16.35 s; its Job was empty and non-aborted, errors were empty, and complete SQL/immutable preservation passed. Five serial observations averaged 113.584 ms for parent validation, 38.466 ms for checked file validation, and 0.579 ms for link validation. Combined checked-open mean was 152.711 ms, read mean 16.795 ms, hashing mean 41.367 ms, and independent total 214.886 ms. All-target Clippy root 43786 exited 0 in 11.46 s; formatting and diff checks passed.

This supports investigating repeated ancestor traversal within one inventory attempt. Any reuse design still needs closing physical-directory checks, replacement/reparse tests, full hashing and byte-limit/error-order preservation. No reuse is implemented by this diagnostic, and no performance acceptance is claimed. Actual release CLI remains 45b1215 with failed cold/hot latency gates. Evidence: target/verification/task6-file-open-diagnostic.log, task6-file-open-diagnostic-statistics.json and the run's raw log, report, Job and preservation records.

### Rejected Windows attribute-only directory experiment

An isolated experiment replaced only the per-component full metadata lookup in checked_canonical_directory with GetFileAttributesW, retaining each component visit and the final canonicalization. It used ordinary/verbatim disk paths, excluded unusual namespace/normalization cases and fell back to the original Rust metadata path after native failure. The API documents that symbolic-link attributes describe the link itself: [Microsoft GetFileAttributesW documentation](https://learn.microsoft.com/en-us/windows/win32/api/fileapi/nf-fileapi-getfileattributesw). The experiment did not cache evidence or remove file identity, hard-link or full-content checks.

Four experimental release tests passed in 0.04 s after a 28.57 s build (root 58418 exit 0): ordinary/Unicode/missing/replaced directory components, unusual-path fallback, long paths, and a real junction including ancestor rejection. An initial test-string escaping compile error was corrected before this run. These tests were removed with the rejected experiment and are not part of retained acceptance counts.

Sequential untraced ABBA root 47215 exited 0. Baseline optimized library E3629253421EB904F197D074BEB815EA7D4AF0202E70B9C1A8F4A89FCF3FED83; experimental library 88834038A818EF846462613678919A93E6693666A0992AFC67A986A6AFC35F73. Runs query-stage-q1oqnI / query-stage-jxnfZh / query-stage-U35NBF / query-stage-FWjbCu all had successful exits, empty non-aborted Jobs, no errors and full SQL/immutable preservation. Ten observations per variant: serial parent-validation mean 114.60 -> 117.62 ms; parallel full inventory 103.51 -> 103.95 ms; controlled-window hot query 211.63 -> 211.20 ms. These observations show no useful gain and do not establish actual CLI latency acceptance.

The experiment was removed completely, leaving production source unchanged. Its optimized binary was archived by hash; the default diagnostic executable was restored byte-for-byte from the E362 baseline. No new CLI was built, no actual CLI or browser result is attributed to the experiment, and latency gates remain false. Do not repeat this attribute-only substitution or treat the directory timing as avoidable without a proven validation design. Evidence: target/verification/task6-directory-attributes-abba-{0,1,2,3}.log, task6-directory-attributes-abba-statistics.json and each owned run's logs, reports, Jobs and preservation records.

### Rejected backlink-discovery overlap and retained capture-phase diagnostic

A bounded experiment overlapped already-directory-checked backlink reads with subsequent discovery directory checks, using three scoped readers plus the caller per four-entry batch. Directory admission stayed caller-ordered, results/errors were consumed in original order after every started reader joined, and payload collection still began only after complete discovery. Four experimental tests covered overlap with exact serial rows/admission, spawn refusal, earlier backlink error precedence over later directory failure, and reader panic. They passed in 0.03 s after a 29.99 s optimized build (root 57100 exit 0). The complete origin-cache set passed 41 tests in 27.75 s (root 14717 exit 0). These counts belong to the subsequently removed experiment, not the final retained source.

Sequential ABBA root 97041 exited 0. Runs query-stage-vY80nl / query-stage-4Qjf7l / query-stage-wvJcmp / query-stage-hKElV8 used baseline E3629253421EB904F197D074BEB815EA7D4AF0202E70B9C1A8F4A89FCF3FED83 and experimental 25CD56F4C937B9268D79D483761E3DEB9B874C3AC885E8A1CC88962B843A3BA7. All Jobs were empty and non-aborted; exits succeeded, errors were empty and complete preservation passed. Ten observations per variant: evidence capture 34.28 -> 34.58 ms, complete proof recheck 37.98 -> 36.63 ms, controlled-window hot query 209.26 -> 209.59 ms. No useful hot-query gain was established, so all production experiment code and its four tests were removed. Do not repeat this backlink-only overlap.

Retained changes are test-only capture-phase clocks using existing tasks_ready and after_origin hooks. They observe the actual candidate and retain its complete Evidence for equality comparison against the acquired proof and the alternating original serial capture. Discovery ends at tasks_ready; parallel payload ends at the last origin guard; finishing includes remaining joins, merge and return work. The diagnostic refuses serial fallback and invalid timestamp ordering. Production contains no new clocks or changed execution policy. Nested phases overlap the enclosing independent evidence-capture timer and must not be added to it.

Optimized diagnostic build root 73067 exited 0 in 80 s, executable SHA-256 05684483C7B47843FA1D1297829C6BC554D26B4CDDAC7A15359190B78D1E6499 (6782af8 plus retained diagnostic). Owned run root 93332 exited 0, query-stage-urkpYK, empty non-aborted Job/root 0, zero errors and full SQL/immutable preservation. Five observations averaged 8.150 ms discovery, 26.207 ms parallel payload, 1.807 ms finishing, nested in a 36.165 ms evidence capture. This directs further investigation toward payload work rather than discovery-only overlap. All-target Clippy root 52729 exited 0 in 12.47 s; formatting/diff checks passed. Actual CLI remains 45b1215/BFB699 with failed latency gates. Evidence: target/verification/task6-discovery-overlap-tests.log, task6-discovery-overlap-abba-{0,1,2,3}.log, task6-discovery-overlap-abba-statistics.json, task6-origin-phase-diagnostic.log, task6-origin-phase-diagnostic-statistics.json and the owned run records.

### Windows kernel no-reparse open prototype (test-only; promising serial result)

The rejected attribute and backlink changes remain absent. A new Windows-only prototype asks NtOpenFile to open a normal disk-relative path beneath an owned drive-root handle, using OBJ_DONT_REPARSE to refuse intermediate reparse traversal. Each call opens/checks the file twice for physical identity and reopens the drive root to compare identity; no directory evidence or file bytes are cached. Returned File values own all handles through RAII. The ordinary inventory hard-link gate and unchanged complete hashing loop remain separate mandatory checks. This is grounded in Microsoft's [NtOpenFile](https://learn.microsoft.com/en-us/windows/win32/api/winternl/nf-winternl-ntopenfile) and [OBJECT_ATTRIBUTES](https://learn.microsoft.com/en-us/windows/win32/api/ntdef/ns-ntdef-_object_attributes) documentation. The prototype deliberately rejects unsupported namespaces, parent traversal and stream syntax; it has no production fallback yet.

All prototype code is cfg(test, windows). The owned diagnostic's additional serial ABBA loop runs only with DEVMAP_QUERY_PROFILE_NOREPARSE=1, after the original complete manifest validation succeeds. Each original/native/native/original sequence checks every file's byte length and SHA-256 against the same frozen manifest and enforces the same aggregate byte limit. The native branch explicitly retains link-count rejection. Ordinary default diagnostic behavior does not invoke this experiment. These serial measurements are not parallel inventory timing, concurrent hot p95 or actual CLI acceptance.

Initial three path tests passed in 0.05 s (root 18099 exit 0, build 28.96 s). Initial complete-corpus diagnostic root 4591 exited 0, run query-stage-eNjmWk, executable C8335379918717D3833679FCACF853534D7F45F43C8F9BFA945FCAC0646EB120. Ten observations each averaged 211.98 ms original versus 105.45 ms prototype. The diagnostic passed in 19.70 s; Job empty/non-aborted/root 0, no errors, complete preservation true. Clippy/build root 91062 exited 0 (11.85 s / 77 s).

The final opt-in version adds live replacement and hard-link checks. An intermediate private-helper compile error was corrected by keeping link-count enforcement/tests within the inventory module, without widening production visibility. Five tests passed in 0.06 s after a 28.71 s optimized build (root 12990 exit 0): Unicode/long files match original bytes and physical identity; an ancestor junction is refused; missing/directory/unsupported inputs are refused; replacement is observed despite an earlier live file handle; both names of a hard link are rejected by the actual prototype inventory opener.

Fresh opt-in diagnostic root 12953 exited 0, run query-stage-0IQfNo, final executable SHA-256 4D473D5C4C2EE02D8805482E2FDF3C5DF152EAA67939EAD5C83700E6750B6374. Ten original observations averaged 215.613 ms (205.934-238.379), ten prototype observations 106.311 ms (100.258-112.446), approximately 50.7 percent less serial elapsed time. Every full byte/hash comparison passed. The diagnostic passed in 19.79 s, Job empty/non-aborted/root 0, no errors, full SQL/immutable preservation true. Final all-target Clippy root 2306 exited 0 in 12.28 s; formatting/diff checks passed.

Next work is handle-lifetime and fault-window coverage, compatibility fallback for unsupported paths/platforms, then integration with the original bounded inventory workers and serial error oracle. Only subsequent current-candidate parallel and actual-CLI measurements can establish latency gains. Production remains 45b1215; no new CLI, browser or final gate is claimed here. Evidence: src/fs_security/noreparse_probe.rs, target/verification/task6-noreparse-prototype{,-opt-in}.log, task6-noreparse-prototype{,-opt-in}-statistics.json and both owned run records.

### Native path opens integrated into bounded inventory workers (480afd4)

The checked no-reparse opener is now production Windows code in src/fs_security/read_no_reparse.rs. Only the speculative inventory workers use it; the original serial inventory/hash opener remains unchanged and remains the complete fallback oracle. Unsupported path syntax returns a decline to the original checked opener; genuine native I/O errors cancel the candidate and reach the existing serial replay. Native reads still perform complete traversal on each call, check opened/reopened file metadata and physical identities, verify the drive-root identity and retain the separate hard-link gate. The original complete hashing loop, global byte reservations, cancellation, joins and canonical result ordering are unchanged. Non-Windows builds keep the original opener. Win32-normalization-sensitive names, device aliases, parent traversal, streams and unsupported namespaces use the original path.

Eight native tests passed in 0.23 s (root 4843 exit 0, build 28.30 s): prior Unicode/long-path/identity coverage plus ambiguous-name decline, replacement between opens, a newly inserted junction to the SAME file identity, and isolated-process handle counts across 128 success/second-open-failure cycles. The first injection attempted to rename a containing directory while its file was open and Windows denied the fixture operation (root 83058: 7 passed, 1 failed). It was corrected by moving the share-delete file first and replacing the empty directory with a junction; this demonstrates rejection beyond a file-identity mismatch. The complete migration library subset passed 86 tests in 94.60 s (root 77665 exit 0), including a native-unsupported parent-traversal path matching the original hash. These 94 focused tests do not replace the still-required current full regression and host/browser checks. All-target Clippy root 36227 exited 0 in 12.24 s; formatting/diff checks passed.

Sequential ABBA root 75403 exited 0, runs query-stage-cZLwHA / query-stage-po5EYS / query-stage-ypOa86 / query-stage-kiNGYF. Baseline 4D473D5C4C2EE02D8805482E2FDF3C5DF152EAA67939EAD5C83700E6750B6374; candidate 14D33CA7C3341B95A01C63B9219FC13AFE8D04DB5A7405DABDF7B43CD5F202B0 (480afd4 implementation built before commit). No opt-in prototype loop was enabled. All Jobs empty/non-aborted, root exits successful, errors empty and complete SQL/immutable preservation true. Ten observations each: complete parallel inventory mean 104.298 -> 83.258 ms, controlled-window sealed hot query 215.871 -> 190.070 ms. Candidate hot range 179.666-194.827 ms. These are in-process measurements, not concurrent CLI p95.

Actual release CLI built after commit 480afd4 in 63 s (root 14846 exit 0), SHA-256 F70ED94BCD2ECDF40073F0D85658A231E1B28FCAE859372BD993FEA867DB0923; the new owned C-drive copy matched the D build. Smoke root 55702 exited 0, run shared-summary-3LeVj1, same fixture/harness/protocol as the preceding CLI smoke. Cold samples 4899.748 / 4880.383 ms, request portions 4364.335 / 4304.032 ms. Four-client hot p95 214.547 / 214.920 / 213.102 / 214.013 ms, three observations each. Maximum result 8781 bytes. All nine owned children exited during harness cleanup, zero spawn/query/cleanup errors and complete preservation true. The actual hot result improved from approximately 239 ms, while cold showed no material improvement. Both latency gates remain false; formal sample populations, current full Rust/browser/native migration/host/resource/freshness acceptance remain outstanding.

Evidence: target/verification/task6-native-open-migration-tests.log, task6-native-open-abba-{0,1,2,3}.log, task6-native-open-abba-statistics.json, task6-480afd4-scale-smoke-config.json, task6-480afd4-scale-smoke.log and corresponding owned run reports, samples, Jobs and preservation records. Further investigation may extend the same kernel traversal mechanism to directory qualification, but requires exact canonical-path output, replacement/reparse behavior and compatibility fallback proof before use.

### Directory qualification prototype with exact canonical output

A test-only directory variant now uses the same full-path kernel no-reparse mechanism around std::fs::canonicalize. It opens the directory and verifies its kind/reparse flags, retains its physical identity, canonicalizes, then reopens and verifies the original path and drive root. If the canonical spelling differs from the input, it additionally opens that returned name through a checked root/no-reparse path and binds it to the first directory identity. This avoids relying only on a pathname sandwich to qualify a differently spelled result. No directory cache is introduced. The production file caller still selects the non-directory flags; no production directory caller uses this prototype.

Ten native tests passed in 0.28 s after a 28.82 s optimized build (root 98973 exit 0). New checks compare ordinary, Unicode and long canonical paths exactly with the original function, refuse missing/non-directory paths, and reject a directory replaced after canonicalization. Existing junction rejection and the isolated-process handle counter now also exercise directory opens. An earlier ten-test build root 57042 exited 0 in 30.80 s with 0.29 s tests before returned-name identity binding was added; final measurements use the hardened version.

The owned diagnostic runs the additional directory experiment only with DEVMAP_QUERY_PROFILE_DIRECTORIES=1. It preserves every repeated parent visit from the same complete manifest, builds expected canonical paths with the original method outside the timed loops, and alternates original/native/native/original. Every returned path must match exactly. The native-file prototype flag was absent. Run query-stage-XhAgfh (root 60929 exit 0) used optimized library SHA-256 B24CE687BAC90120BA6C2A0326479C8A7F34743018EFF3B415C24DD1D7CF4ECD, eaaf97e plus directory prototype. Ten original samples averaged 105.343 ms (101.890-109.724); ten native samples 64.631 ms (60.645-69.665), approximately 38.6 percent lower for this serial directory-call population. These times are not additional savings measured in a production query and must not be applied directly to CLI p95.

The diagnostic passed one test in 18.05 s, Job empty/non-aborted/root 0, no errors and complete SQL/immutable preservation. All-target Clippy root 87858 exited 0 in 11.86 s; formatting/diff checks passed. Evidence: target/verification/task6-directory-prototype.log, task6-directory-prototype-statistics.json and the owned run's raw log, report, Job and preservation records. The preceding 14D33CA7... library was preserved by hash before building. Actual CLI remains 480afd4/F70ED94B..., with both latency gates false.

Next integration should retain the original serial directory provider and try the candidate provider in parallel origin evidence collection and reciprocal-link validation. Unsupported syntax or access behavior needs compatibility fallback; opening/closing proof boundaries and full canonical output/physical identity must remain. Current full regression, browser/host and formal performance gates remain outstanding.

### Rejected directory integrations: local savings did not transfer to hot queries

Two bounded integration experiments were evaluated and removed. The first used the native directory provider only in parallel origin evidence and its reciprocal-link checks; original serial evidence retained the original provider. Unsupported/native-error results got one fresh original qualification. Ten path tests and 87 migration tests passed (combined root 82560 exit 0; optimized build 76 s, tests 0.30 / 95.26 s), including an injected native-only permission failure matching the original provider. An earlier build root 36021 exited 0 in 77 s.

Origin-provider ABBA root 3034 exited 0: query-stage-xtUimc / query-stage-wPUemW / query-stage-X47PtN / query-stage-0kyyd3. Baseline B24CE687BAC90120BA6C2A0326479C8A7F34743018EFF3B415C24DD1D7CF4ECD versus experimental 1F5160F43B31D5BC4AAF1D53D731772AB419461A7964C88588D845A29C01AE48. All Jobs empty/non-aborted/root 0, no errors and complete preservation. Ten observations each: evidence capture 34.118 -> 40.847 ms; payload 25.150 -> 32.919 ms; discovery 7.160 -> 6.235 ms; sealed hot query 183.550 -> 208.360 ms. This is a measured regression for that integration; no exact syscall-level cause is claimed. The entire origin-provider change was restored before trying a different scope.

The second experiment selected native directory qualification only for the parallel inventory producer's walk; serial inventory and the independent metadata-only collector explicitly retained the original provider. The same fresh original fallback and its test were scoped to inventory. Migration tests passed 87 tests in 94.89 s (root 98871 exit 0, build 79 s). Inventory-provider ABBA root 73318 exited 0: query-stage-zFol6I / query-stage-pexbMV / query-stage-fX3Bc4 / query-stage-lTWEzt, baseline B24CE687... versus 7CCAF4F644B2A58A34D5489019CA01877766E9E4D6F407C98FF76A3E0340DD4A. All Jobs empty/non-aborted/root 0, no errors and complete preservation. Ten each: parallel inventory 82.712 -> 79.198 ms; sealed hot query 185.507 -> 188.275 ms. The small component reduction did not establish useful whole-query improvement. All-target Clippy root 68230 exited 0 in 12.03 s on this experimental source.

Both integrations and their extra fallback test were removed; the original 31eaa19 source, including its test-only directory prototype, was restored. The default diagnostic executable was restored and verified as B24CE687..., with the earlier 14D33CA7... and B24CE687... hash-named baselines retained. The final rejected patch is saved in target/verification/task6-inventory-directory-rejected.patch. No new actual CLI was built or measured: production remains 480afd4/F70ED94B..., actual hot p95 approximately 213-215 ms and cold approximately 4.9 s, both gates still false. Do not apply the isolated directory prototype's 38.6 percent reduction to either real query path or repeat these integrations without new evidence.

Evidence: target/verification/task6-native-directory-{path,migration}-tests.log, task6-native-directory-abba-{0,1,2,3}.log, task6-native-directory-abba-statistics.json, task6-inventory-directory-migration-tests.log, task6-inventory-directory-abba-{0,1,2,3}.log, task6-inventory-directory-abba-statistics.json, the saved rejected patch and each owned run's reports/raw logs/Jobs/preservation records. Further work should prioritize the still-dominant cold initialization/collection path rather than extending a directory primitive based on isolated timings.

### Exact-bound query configuration reuse for cold origin acquisition (967a654)

A verified query may now supply its immutable configuration evidence to the active SQL reader's initial origin acquisition. The application still uses its own anchor workspace, which can differ from the query client. Root, Git admin and common directories are freshly qualified and must match the retained configuration's canonical bindings. The configuration is rechecked before authoritative origin enumeration, after that enumeration, and again after observation. Different anchors, stale evidence and failed path qualification retain original acquisition/fallback behavior. Existing origin evidence, complete legacy hashing, SQL generations, leased query ownership and error invalidation remain unchanged. Large configuration/source witnesses are shared with Arc; bounded values/path bindings are copied. No successful validation boolean is retained or reused.

The first focused run (root 64918 exit 101) passed 12 tests but failed the new exact-binding optimization assertion: ordinary Windows inspector paths differed from checked canonical/verbatim paths, causing a safe fallback and no Git-start saving. The fix freshly qualifies each supplied binding before comparison, rather than accepting lexical aliases without checking them. The final origin subset (root 13817 exit 0) passed 13 tests in 50.94 s after a 90 s optimized build. It covers main and linked worktrees, same-anchor three-start savings, other-anchor/stale fallbacks matching every storage input and fingerprint, and configuration changes during observation discarding the proof. Query-validation regression (root 59749 exit 0) passed 25 tests with 1 explicitly ignored diagnostic in 111.06 s. All-target Clippy root 86198 exited 0 in 13.18 s; formatting/diff checks passed. These subsets do not replace final full regression.

In-process ABBA root 35235 exited 0: query-stage-koIZyE / query-stage-G0UjvF / query-stage-Ez3oU1 / query-stage-oHlUXT. Baseline B24CE687BAC90120BA6C2A0326479C8A7F34743018EFF3B415C24DD1D7CF4ECD, candidate CF3A62F6A5E27086C9DC6D8F40C8481728413F001F8712D9B724A21C62EF2E39 (967a654 implementation built before commit). Every Job empty/non-aborted/root 0, errors empty and complete preservation true. Two initial samples each: mean 3779.579 -> 3678.199 ms and Git starts 67 -> 64. Ten controlled-window hot samples each: 205.787 -> 204.232 ms. This establishes a bounded process-count reduction and a small sampled cold gain; it does not establish a hot improvement or formal p95.

Actual CLI built after commit in 70 s (root 92634 exit 0), SHA-256 CA6EA842C27C48CBB24A88C12AAABFA35EF21BCD410CA209F762C529812F1779, copied to target/verification/task6-candidate-967a654 with matching hash. Initial smoke root 67788 exited 0, shared-summary-tcB3Vh: cold 4806.660 / 4679.930 ms, request portions 4214.146 / 4154.489 ms, per-client hot p95 243.077 / 243.592 / 242.037 / 244.099 ms, 8781 bytes maximum. No query/spawn/cleanup errors, nine owned exits observed, preservation read cohorts true. Both latency gates remain false.

Because this hot result was slower than earlier historical measurements, immediate old-CLI comparisons were scheduled. Root 93407 completed baseline shared-summary-74nuqg and shared-summary-JDBM95, then exited 1 before its final candidate could start: the harness's 256 MiB reserve plus candidate-copy check rejected insufficient D-drive space. No child or run directory was created for that rejected final sample. The incomplete sequence must not be called a completed ABBA comparison. New comparative runs use one newly allocated C-drive artifact parent for BOTH binaries, with unchanged source fixture, receipt, policy and preservation checks; do not mix the output-location change into a same-environment performance claim.

Evidence: target/verification/task6-config-reuse-origin-tests{,-final}.log, task6-config-reuse-query-tests.log, task6-config-reuse-clippy.log, task6-config-reuse-abba-{0,1,2,3}.log, task6-config-reuse-abba-statistics.json, task6-config-reuse-cli-build.log, task6-967a654-scale-smoke-config.json, task6-967a654-scale-smoke.log and task6-config-reuse-cli-comparison-{0,1,2}.log. Current native browser/host/full regression and formal performance/resource/freshness acceptance remain open.

C-drive actual-CLI ABBA root 52786 exited 0. Artifact parent C:/Users/user/.devmap-test-fixtures/devmap-sqlite-compatible-20260909/summary-runs-96fb52d407c7462e9ecd321993bc4279; runs shared-summary-OTy9Xa / shared-summary-WeWl4E / shared-summary-TTMDQI / shared-summary-8dHA9x. Both versions used this same parent and unchanged source fixture/receipt/harness policy. Four cold samples each averaged 4919.229 ms baseline versus 4725.673 ms candidate. Baseline cold samples 4821.974 / 4912.264 / 4981.831 / 4960.850 ms; candidate 4850.426 / 4688.082 / 4684.645 / 4679.538 ms. Each run had four clients with three hot observations per client. Baseline per-client p95 values ranged 232.779-243.404 ms; candidate 241.941-265.668 ms. The candidate therefore has a hot-regression signal in this small comparison despite a cold gain; do not claim hot equivalence or acceptance. Averaging these separate small-sample p95 values is not a pooled p95 or formal acceptance statistic.

All four runs completed with no query/spawn/cleanup errors, nine observed owned exits each, preservation_read_cohorts true and maximum result size 8781 bytes. All cold/hot/overall gates remained false. The earlier D-drive baseline samples also showed approximately 239-251 ms hot values, so historical 213-215 ms is not a valid contemporaneous control. Current code inspection confirms the closed cohort executes once for a matching query and the epoch branch precedes the new configuration-supplied fallback; the existing active SQL test passed its exact two-source-capture assertion. This does not determine the cause of the observed CLI regression. Keep that concern open before final acceptance; current candidate is not a completed delivery. Evidence: target/verification/task6-{480afd4,967a654}-cdrive-smoke-config.json, task6-config-reuse-cdrive-abba-{0,1,2,3}.log, task6-config-reuse-cdrive-abba-statistics.json and the four owned run reports/raw observations. D-drive free space is approximately 266 MB; use the owned C-drive artifact allocation for subsequent runtime tests, without weakening reserve checks or deleting old evidence.

### Buffered hot-query boundary and projection attribution

Test-only opt-in modes boundary_phases and projection_phases now run ten hot queries after one owned cold query, with a controlled 60-second collection window. Every query must start zero Git processes, perform exactly two complete source captures, and match every serialized ApplicationSnapshot field after normalizing only model.generated_at. Each mode buffers its phase rows until its measured operation finishes. The two modes run separately so nested logging does not contaminate the other mode's enclosing stage. Timings are in-process diagnostics, not actual concurrent CLI acceptance.

Boundary build root 42889 exited 0 in 87 s. Owned run query-stage-h1kMFe, library SHA-256 57C281D75C853EB23484C4903B5C1634460B42FD9ED5D774170F7F72B52DFD87, passed in 6.12 s; Job empty/non-aborted/root 0, no errors, complete preservation true. Ten means: opening boundary 41.099 ms, closing boundary 39.153, projection 116.606, eligibility 1.211, opening anchor 1.390, closing anchor 2.400, restore 0.002. Complete measured query mean 205.017 ms (197.916-227.224). All-target Clippy root 66505 exited 0 in 11.77 s.

Projection build root 32639 exited 0 in 85 s. Owned run query-stage-JVKvyO, library SHA-256 ACD37FD5CA2A908604DBCB93DE862DC622340B1F81E9A55B51B059DB5EFA571C, passed in 6.20 s; Job empty/non-aborted/root 0, no errors, full preservation true. Ten means: storage read approximately 112.26 ms, source/anchor 2.63, client preparation 1.35, model projection 6.86; collection eligibility and snapshot finishing each below 0.01 ms. All-target Clippy root 3439 exited 0 in 12.16 s. The prior CF3A62F6... library was preserved under target/verification/task6-candidate-967a654 before rebuilding. The ignored owned wrapper now accepts an explicitly checked artifact parent outside the source allocation; both diagnostics used the existing owned C-drive allocation.

These results rule out an observed missed query epoch in the ten diagnostic calls and locate most hot time in storage checks plus the opening/closing origin proof. They do not explain the actual CLI comparison's regression signal, and do not establish actual p95 equivalence. No new actual CLI was built: it remains 967a654/CA6EA842..., with cold/hot and all final gates still open. Next bounded experiment may evaluate the existing native FILE opener in parallel origin payload reads; the unchanged serial evidence path must remain the fallback, and no directory-provider experiment should be repeated. Evidence: target/verification/task6-{boundary,projection}-profile{,-statistics}.json or .log as applicable, both build/Clippy logs, and the two owned run reports.

### Rejected native FILE opener for parallel origin payloads

A separate experiment changed only Collector.file's first and post-read file opens in origin_cache_parallel.rs to the existing Windows no-reparse file opener. Unsupported syntax retained the original checked opener; native errors reached the existing complete serial evidence fallback. All original metadata checks, admission limits, file bytes, file identities and serial Evidence::capture remained. No new hard-link restriction was added to origin evidence and no directory qualification provider changed.

Migration subset root 61699 exited 0: optimized build 92 s, 86 tests passed in 97.43 s. Initial all-target Clippy rejected a redundant map_err(Into::into) on an already-correct DevMapError; removing that identity conversion passed final Clippy root 58148 in 12.85 s. A final optimized measurement build root 56607 exited 0 in 93 s. The 86-test run preceded only that redundant-conversion removal; the final binary additionally passed every owned diagnostic's complete query parity, two-source-capture, zero-Git and preservation checks.

Boundary-mode ABBA root 1724 exited 0, C-drive owned runs query-stage-JGWFRH / query-stage-29cvHf / query-stage-ylztdB / query-stage-0R0RjS. Baseline ACD37FD5CA2A908604DBCB93DE862DC622340B1F81E9A55B51B059DB5EFA571C versus candidate B1F66FD697C041BD36A5B42B713240CB1155CFD53CF4FA8E873A4FD1F075115A. Every Job empty/non-aborted/root 0, no errors and complete SQL/immutable preservation. Twenty samples each: complete hot-query mean 209.279 -> 232.227 ms, opening boundary 41.627 -> 50.945 ms, closing boundary 41.089 -> 51.110 ms. This is a clear regression for this integration, distinct from the earlier rejected directory experiments. The existing native file opener's inventory benefit does not transfer to this origin payload call site.

The entire one-file production experiment was restored to 6bcf2ac. Its patch is retained at target/verification/task6-origin-native-file-rejected.patch. Default optimized library was restored from the retained ACD37FD5... copy and hash-verified. The rejected B1F66FD6... binary was overwritten and is not archived. No actual CLI was built from the rejected experiment; actual CLI remains 967a654/CA6EA842..., and the earlier unresolved actual-CLI hot comparison and all final acceptance gates remain open. Future optimized builds must rebuild against restored source rather than trust stale Cargo artifact metadata.

Evidence: target/verification/task6-origin-native-file-tests.log, task6-origin-native-file-clippy{,-final}.log, task6-origin-native-file-final-build.log, task6-origin-native-file-abba-{0,1,2,3}.log, task6-origin-native-file-abba-statistics.json, rejected patch and all four owned run reports. Next work should use the established stage costs to investigate collection or storage structure; do not repeat direct native file/directory substitutions without a new reason supported by measurements.

### Storage read attribution

The test-only storage_phases mode uses the same ten-query, complete-result, zero-Git and exactly-two-source-capture diagnostic. Owned run query-stage-dhCkea used library DCC7DBA39638220DEA59220829EC6CDCE3C1A97368609D1582679606E117E704 after an 82 s build (root 44922 exit 0). Job empty/non-aborted/root 0, no errors, complete preservation true. Ten means: origin observation 102.93 ms, presence/route/binding qualification 2.36, database path/identity 1.67, snapshot/schema 0.28, commit/cache 0.12, cached input cloning 0.10. These are separate measurements from boundary/projection modes, not additive cross-run costs or concurrent CLI p95. The SQL and clone portions are too small to explain the latency gap; no SQL-cache bypass is introduced. No new production behavior or actual CLI is claimed. Evidence: target/verification/task6-storage-profile{,-statistics}.json or .log as applicable, task6-storage-profile-build.log, task6-storage-profile-clippy.log and the owned run report.

### One opening shared-facts capture with a full closing comparison (9360743)

Shared Git facts now retain one opening candidate capture, compute representative facts and exact tag witnesses, and perform the unchanged full closing evidence comparison before publishing any shared rows. The formerly consecutive second pre-computation capture was removed. The private type is renamed SharedFactsCandidate to distinguish an opening observation from successful publication qualification. Every capture still checks the complete original configuration, refs, worktree file evidence, physical identities and closing directory identities. The final tag rereads, complete capture comparison, immutable operands, original-report fallback, inherited Git deadline and fatal-error precedence remain. Empty/failed attempts publish no shared results. This change does not alter hot-query source validation, SQL/legacy hashing, or the frozen frontend.

A new real-fixture fault inserts a tag after the opening capture, before representative computation. Representative/tag reads therefore see the new tag; only the full closing comparison can reject the older opening evidence. The test requires zero shared rows/groups, equality with the fresh original report, and a subsequent stable invocation that successfully shares facts. Together with existing post-computation tag drift, configuration/ref/directory changes and real deadline tests, the shared-facts subset passed 36 tests in 48.83 s (root 32434 exit 0, optimized build 83 s). All-target Clippy root 13690 exited 0 in 13.09 s.

Collection ABBA root 43871 exited 0, owned C-drive runs query-stage-QayMg2 / query-stage-asbQZO / query-stage-0cOdPn / query-stage-SPAU8n. Baseline DCC7DBA39638220DEA59220829EC6CDCE3C1A97368609D1582679606E117E704, candidate 7E022101304A8AA067DCC3452F91BD0F4D5F813BC0980A81A0489CE2D3FD5848 (9360743 implementation before commit). All Jobs empty/non-aborted/root 0, no errors and full preservation. Six samples each: complete collection mean 2250.112 -> 2032.249 ms; relationship stage 1520.064 -> 1303.604 ms, Git starts 45 -> 40; shared-facts stage 961.295 -> 766.650 ms, Git starts 20 -> 15. Every complete worktree/topology/relationship/configuration comparison remained equal within its owned diagnostic. These are in-process collection measurements, not CLI acceptance.

Actual CLI built after commit in 75 s (root 86582 exit 0), SHA-256 04D85FC68C4AE08200D7D2F139266D27A8297E8E1BE5DBDA3E7CD5928FC755CB; C-drive copy target/verification/task6-candidate-9360743 matched. Smoke root 69916 exited 0, shared-summary-4XV6Bj: cold 4536.168 / 4549.142 ms, request portions 3998.317 / 3977.842 ms; four-client hot p95 258.008 / 259.937 / 260.909 / 261.589 ms, three observations each. Maximum 8781 bytes, no query/spawn/cleanup errors, nine owned exits observed, preservation_read_cohorts true. Both latency gates remain false. The earlier actual-CLI hot concern is not resolved by this cold-only change. Full all-target regression was started on source 9360743; its separate terminal outcome is required before claiming it passed.

Evidence: target/verification/task6-shared-capture-tests.log, task6-shared-capture-clippy.log, task6-shared-capture-abba-{0,1,2,3}.log, task6-shared-capture-abba-statistics.json, task6-shared-capture-cli-build.log, task6-9360743-cdrive-smoke-config.json and task6-9360743-cdrive-smoke.log. The DCC7DBA3... baseline remains retained by hash on D. Existing C-drive owned runtime artifact allocation is used because D has less than the runtime harness reserve; reserve checks are unchanged.

## Current scale freshness smoke at 9360743

The actual retained CLI 04D85FC68C4AE08200D7D2F139266D27A8297E8E1BE5DBDA3E7CD5928FC755CB completed the four-client freshness smoke (root 4546, exit 0) on the existing schema-2 receipt with 20 worktrees, 100 sessions and 100000 events. Run shared-freshness-IK5d2S used the owned C-drive summary-runs allocation. Two warmup changes preceded four measured dirty/clean changes, yielding all 16 client detections. Each response had to match the expected status, original HEAD and a strictly newer Git cycle; no replacement samples were used.

Client p95 values were 4282.7552, 4285.0807, 4281.1136 and 4284.1926 ms; the per-trial maximum p95 was 4285.0807 ms. The 2000 ms gate failed. This is a four-change smoke, not the required 100-change acceptance population. The full Rust regression was running concurrently, so these values are diagnostic and cannot isolate a regression against an earlier binary. No baseline comparison or uncontended timing is claimed.

All four trials completed without request or cleanup errors. The probe's original bytes and clean status were restored; full SQL, immutable backup/source inventories and Git HEAD/refs/index/config preservation checks passed. All five retained children exited during harness cleanup. This is not natural-idle or resource evidence. The production application uses a two-second maximum collection age; collection starts after expiry and itself takes time. The observed latency therefore motivates examining change-triggered refresh and collection costs while retaining the existing freshness and identity proofs; simply reducing SQL parsing cannot establish this gate.

Evidence: target/verification/task6-9360743-freshness-smoke.log and C:/Users/user/.devmap-test-fixtures/devmap-sqlite-compatible-20260909/summary-runs-96fb52d407c7462e9ecd321993bc4279/shared-freshness-IK5d2S/report.json. Production code was unchanged by this measurement.

The retained poll timestamps further separate waiting before the converging request from that request's complete response latency. For client zero across the four trials these pairs were 610.9625/2362.3036, 1710.4356/2497.3563, 1808.3705/2292.3410 and 1812.2845/2470.4707 ms. Other clients' rows are preserved in target/verification/task6-9360743-freshness-request-breakdown.json. The second quantity includes request processing, IPC and response validation; it is not an isolated collection timer. Even immediate dispatch would not bring those observed request durations below 2000 ms. Change hints alone are therefore insufficient in this diagnostic run; collection cost must also fall. These values retain the concurrent-regression caveat above.

## Current default idle resource acceptance at 9360743

Resource root 58786 terminated with exit 0. The current retained release 04D85FC68C4AE08200D7D2F139266D27A8297E8E1BE5DBDA3E7CD5928FC755CB used the unchanged 20-worktree, 100-session, 100000-event receipt and owned C-drive run shared-freshness-oWEbiP (the report schema is devmap/shared-summary-resources/1 despite the directory prefix). Four proxies completed ten warmup rounds and a cursor audit, then closed before a retained native process-handle observer sampled 600 seconds at five-second intervals. All 121 samples were present.

Owner PID 19752 exited naturally with code 0, no signal and no harness termination request at 2026-09-13T05:29:47.254Z. Native exit time gives an active interval of 60.7853503 seconds, within the last-alive/first-exited sample bounds. Reported CPU counters did not increase during the idle window: both active-interval and full-window CPU estimates are zero at the counter's resolution, not proof of literally zero execution. Sampled peak RSS was 24596480 bytes; recorded lifetime peak was 25100288 bytes (23.9375 MiB), including startup. These are owner-only measurements and exclude proxies, Git descendants and observation tools.

The default_idle_resource_acceptance field is true. continuous_residency_acceptance is false because the default core exited; performance_acceptance remains false. All SQL and immutable source/backup preservation checks passed after the window and cleanup. There were no request, preservation or cleanup errors, and all six retained child processes have exit records. The full Rust regression overlapped setup and the early idle window, terminating around 05:32:28Z; no timing-isolated system-wide resource claim is made. Evidence: target/verification/task6-9360743-resources-config.json, task6-9360743-resources.log and C:/Users/user/.devmap-test-fixtures/devmap-sqlite-compatible-20260909/summary-runs-96fb52d407c7462e9ecd321993bc4279/shared-freshness-oWEbiP/report.json and resources.json.

## Retained topology-key probe overlap

The topology key now overlaps its independent for-each-ref and shallow-repository reads using the existing scoped probe-pair helper, moved to git_process/probe_overlap.rs and reused by shared_facts. It retains the complete command arguments, refs-first byte concatenation and ordinary error order, shallow refusal, and both opening/closing collection checks. Started supervision failures remain fatal; workers inherit the absolute Git budget and are joined before returning. Thread-spawn refusal uses the original serial short circuit. No TTL, filesystem witness or Git concurrency limit changed.

The moved helper's five tests passed (root 63765, exit 0), including overlap/budget inheritance, error precedence, spawn refusal, panic cleanup and real expired Git admission. Integration root 43451 exited 0: dock_model 38 passed, git_topology 17 passed and repository_application 10 passed. Clippy with all targets and denied warnings passed (root 51322, 10.01 seconds); formatting and diff checks passed. Optimized library build root 39054 exited 0 after 103 seconds, SHA-256 4F09BB4A6730D659B489DC5939261E41344BFBD39CB969AA81E188791F9B467B. The pre-change 9360743 library 7E022101304A8AA067DCC3452F91BD0F4D5F813BC0980A81A0489CE2D3FD5848 was preserved by hash before the build.

After builds and integration tests terminated, ABBA root 53541 exited 0 using the fixed scale receipt and owned C-drive run parent. Runs vjn5vB/PLtFj1/BTfLtd/Qgg4Hc all had empty owned Jobs, zero errors, no aborts, exit 0 and complete preservation. Six samples per version gave opening-key means 136.9132 to 70.6977 ms, closing-key means 132.3973 to 68.7770 ms, and total collection means 2070.6345 to 1897.2547 ms. Each diagnostic verifies unchanged worktrees, topology, relationships and configuration between its iterations; it uses no route targets and is not a cross-version full model oracle or actual CLI latency test. The focused gain supports retaining this candidate, but actual CLI, browser and full regression results at 9360743 do not yet cover the new change. Evidence: target/verification/task6-topology-key-overlap-{unit,integration,clippy,release-build}.log, task6-topology-key-overlap-abba-{0,1,2,3}.log and task6-topology-key-overlap-abba-statistics.json.

After commit 15a487c, actual CLI build root 20627 exited 0 in 71 seconds. The D release and retained C target/verification/task6-candidate-15a487c/devmap.exe matched SHA-256 B59AF04B90439493BE1F21001773143EE90484165172C528B0CF73A00FFF83F2. Actual CLI smoke root 21969 exited 0, run shared-summary-h0TEgP on the same C-drive scale receipt: two cold opens were 4382.9717 and 4393.3645 ms; four clients' hot p95 values were 252.0216, 253.6751, 252.8888 and 250.1880 ms (three observations each). Maximum response was 8781 bytes. There were no errors or cleanup failures and preservation_read_cohorts was true. Both latency gates remain false. This is not a formal population or a contemporaneous actual-CLI ABBA comparison; current full Rust, native browser and idle-resource evidence still belongs to 9360743. Evidence: target/verification/task6-15a487c-cli-build.log and task6-15a487c-cdrive-smoke-{config.json,log}, plus shared-summary-h0TEgP/report.json.

## Retained shallow-boundary and history-read overlap

After discovering nonempty traversal tips, GitTopologyCollector now uses the same scoped probe-pair helper to overlap its shallow-boundary read and bounded commit-history read. Both results are joined before constructing any boundary, parent edge or commit. The history traversal does not consume the separately read boundary set. Empty repositories retain the previous early return, command arguments and history limits are unchanged, ordinary errors remain shallow-read first, and started supervision failures remain fatal. This does not introduce a stronger atomic Git snapshot claim; the surrounding collection checks are unchanged.

Focused test root 64587 exited 0: dock_model 38 passed in 132.94 seconds and git_topology 17 passed in 31.32 seconds, including shallow history, omitted parents, tags, partial clone and read-failure behavior. All-target Clippy with denied warnings passed (root 16413, 9.52 seconds), as did formatting/diff checks. Optimized-library build root 2641 exited 0 in 105 seconds, SHA-256 389FE4022A12A10810F57723D47400963FD803915B59880CA214EC4FD77A57FB. Baseline 15a487c library 4F09BB4A6730D659B489DC5939261E41344BFBD39CB969AA81E188791F9B467B was retained by hash before rebuilding.

After tests and compilation completed, ABBA root 25241 exited 0 with runs DRPweN/ZuZpCQ/fLWgFD/Wa8fHR on the same scale fixture and owned C-drive parent. Every run had an empty/non-aborted owned Job, exit 0, no errors and complete preservation. Six samples per version gave topology means 248.9768 to 186.2870 ms and total collection means 1893.6570 to 1846.8495 ms. As above, this diagnostic uses no route targets, verifies stable full collection structures within each run, and does not prove actual CLI or cross-version browser equivalence. Actual CLI latency evidence remains at 15a487c until rebuilt and measured; broader current acceptance remains open. Evidence: target/verification/task6-topology-history-overlap-{tests,clippy,build}.log, task6-topology-history-overlap-abba-{0,1,2,3}.log and task6-topology-history-overlap-abba-statistics.json.

### Actual db2abdf CLI smoke

Build root 68780 exited 0 in 72 seconds. The D release and retained C target/verification/task6-candidate-db2abdf/devmap.exe matched SHA-256 13D8B43BEDE032A44FD8827916EC57059A45021B70B2116EF87E1C03D4F12395. Actual smoke root 83422 exited 0, run shared-summary-Y9DIZF on the same scale receipt and C-drive parent. Cold opens were 4370.4497 and 4388.3277 ms; four-client hot p95 values were 237.2572, 235.5387, 236.4397 and 237.7024 ms, three samples each. Maximum response remained 8781 bytes. Errors and cleanup errors were empty, and preservation_read_cohorts was true. Both latency gates remain failed. These are smoke results, not formal acceptance or an actual-CLI ABBA comparison. Logs: target/verification/task6-db2abdf-cli-build.log and task6-db2abdf-cdrive-smoke.log; configuration is task6-db2abdf-cdrive-smoke-config.json.

### Rejected native file opens for source-resolution witnesses

A separate uncommitted experiment used the existing Windows no-reparse file opener only for SourceResolutionWitness file witnesses, with original opening on unsupported syntax. It retained bounded full bytes, both file identities, directory closure and exactly two source captures; the generic original witness path stayed unchanged. This is distinct from the previously rejected origin-payload native-file call site. Three complete-source-evidence tests and 25 query-validation tests passed, with one explicit diagnostic ignored (root 20762, exit 0); Clippy passed (root 45652, 9.80 seconds). Optimized build root 51166 exited 0 in 96 seconds, experimental SHA-256 317DE3122946B19AD849C87B6BA66EB31410EC961DF0A091E704AD89F2363397.

ABBA root 96459 exited 0 after tests/builds terminated. Runs YnBmDg/jFNzZS/rxLxwJ/mYmqCz compared baseline library 389FE4022A12A10810F57723D47400963FD803915B59880CA214EC4FD77A57FB against the experiment on the same scale receipt. All Jobs were empty/non-aborted with root exit 0, no errors and complete preservation. Twenty hot observations per version retained zero Git starts, exactly two source captures and full serialized snapshot parity within each run after generated_at normalization. Opening-boundary means were 41.17325 to 41.74370 ms, closing means 40.85905 to 39.93705 ms, and total means 205.75125 to 204.03025 ms. The tiny overall difference and lack of consistent boundary improvement do not support retaining the extra path.

Both source files were restored exactly to db2abdf and the default optimized library was restored from the retained 389FE4 hash-named baseline. The experimental binary was overwritten and is not archived; no actual CLI used the experiment. Evidence remains in target/verification/task6-source-native-file-{unit,query,clippy,build}.log, task6-source-native-file-abba-{0,1,2,3}.log, task6-source-native-file-abba-statistics.json and task6-source-native-file-rejected.patch. Production remains db2abdf; do not repeat this source-witness opener experiment as an untested new direction.

### Rejected bounded source-ref payload batches

A separate experiment retained the original file/path witnesses but collected bounded ref paths first, then read four files at a time with the existing scoped probe-pair helper. Fragments inherited directory identities freshly discovered in the same capture, merged in path order, rejected conflicting identities and retained full byte and final directory checks. The revised evidence test compared 24 batched files plus an absent file with the original complete Evidence. No native file opener was used.

An initial concurrent test rebuild hit Windows LNK1104 because the original query test executable was still running (root 79944, exit 101); no assertion ran in that attempt. The original query run completed, and subsequent validation was sequential. Final seeded chain root 86896 exited 0: three evidence tests passed in 0.05 seconds, 25 query-validation tests passed with one explicit diagnostic ignored in 101.34 seconds, all-target Clippy passed in 8.79 seconds and the optimized build completed in 88 seconds. Experimental library SHA-256 E8C204A45D98044447DA8255C5834022843BE56A288BF30B80AE0CBAF6A37CFE.

ABBA root 49655 exited 0 after the validation chain terminated. Runs mfPgeb/3vjHhZ/vgkmAl/03adZU compared baseline 389FE4 against the experiment using the same scale receipt. All Jobs were empty/non-aborted with exit 0, no errors and complete preservation. Twenty hot observations per version retained zero Git starts, two source captures and full serialized snapshot stability after generated_at normalization. Opening means were 39.9412 to 40.74535 ms, closing means 39.59605 to 39.6624 ms, and total means 199.7454 to 200.86565 ms. The fixture has twenty loose branch refs, so this was not merely an empty or packed-ref path. No end-to-end gain supports keeping the added batching machinery.

The complete source change was restored to db2abdf, and the default optimized library was restored from the 389FE4 baseline and hash-verified. The experimental binary was overwritten and unarchived; actual CLI 13D8B43B remained unchanged. Evidence: target/verification/task6-source-ref-batch-seeded-{evidence,query,clippy,build}.log, task6-source-ref-batch-abba-{0,1,2,3}.log, task6-source-ref-batch-abba-statistics.json and task6-source-ref-batch-rejected.patch. The initial failure and successful rerun logs are retained separately.

Further code inspection confirms that query_boundary already overlaps configuration/source rechecking with origin-proof payload capture, so improving only one side need not reduce the boundary duration. Also, observe_in_query_epoch still invokes the complete active-origin observer: cached origin enumeration does not bypass activation validation or the full legacy inventory call. Subsequent work should attribute that complete observer before repeating per-ref file-opening or batching experiments; this entry proposes no weaker freshness or legacy-drift policy.

### Complete active-origin observer attribution

Added opt-in, test-only buffered observer phase timing and an explicit hot-start marker. The owned scale query diagnostic retains ten hot queries, zero Git starts, exactly two source captures, full serialized snapshot equality after generated_at normalization, receipt validation and complete SQL/immutable-root preservation. Production behavior remains db2abdf.

Optimized build root 75645 exited 0 in 87 seconds, library SHA-256 5DDC369D45D3C336FE28427E65645937290EC12CA9A180972A084FC741CE697E. Formatting and wrapper syntax checks passed; all-target Clippy with denied warnings completed in 9.03 seconds. Run query-stage-DLY0xK exited 0 with an empty, non-aborted owned Job (root 19928), no errors and preservation true. Ten-sample phase means in milliseconds: activation validation 3.5041, opening enumeration 0.0076, origin classification 0.0334, legacy inventory 85.8393, inventory comparison 2.2998, closing enumeration 0.0100, fingerprint/report 0.0259. Inventory accounts for about 94% of the 91.7201 ms phase sum. This is attribution, not an optimization or formal latency acceptance; outer totals include diagnostic output and must not be compared directly with other diagnostic modes.

The next measurement should split serial discovery plus overlapping workers from the remaining join and merge time. The existing four-worker inventory pipeline already overlaps discovery and full payload hashing; prior eight-worker and native-directory experiments remain rejected. Evidence: target/verification/task6-observer-profile-{build,clippy}.log, task6-observer-profile.log and task6-observer-profile-statistics.json. Current actual CLI latency gates and broader acceptance remain open.

### Inventory pipeline attribution

Added an opt-in test-only inventory_phases mode with buffered marks around serial discovery plus overlapping hashes, remaining scoped worker joins, and ordered merge/sort. This retains the original queue, cancellation, fallback, hashing and production control flow. Formatting, wrapper syntax and all-target Clippy with denied warnings passed (8.80 seconds). Build chain root 68235 exited 0; optimized library build completed in 87 seconds, SHA-256 7E6112DCFE0FD88585B6A0609266486407159A639FF9037FC29272EE64335CC8. This diagnostic build supersedes the default test executable 5DDC369D; actual CLI 13D8B43B remains unchanged.

Owned scale run query-stage-iOYD6b exited 0, with empty/non-aborted Job (root 9196), no errors and complete preservation. Ten hot queries retained zero Git starts, two source captures and full serialized snapshot stability after generated_at normalization. Discovery plus overlapping hashes averaged about 88.33 ms; remaining joins about 0.79 ms; merge/sort about 0.03 ms. These are wall-clock phases and must not be added to independent worker CPU totals. The discovery phase includes bounded-queue backpressure, so the result alone does not distinguish metadata discovery from waiting for payload hashing. The next useful measurement is cumulative producer queue-wait duration within that phase, before changing the pipeline. No performance improvement or formal acceptance is claimed.

Evidence: target/verification/task6-inventory-phases-{clippy,build}.log, task6-inventory-phases.log and task6-inventory-phases-statistics.json. The current production db2abdf actual-CLI smoke still fails both cold and hot latency gates; broader current acceptance remains open.

### Producer queue backpressure attribution

Added test-only, opt-in cumulative producer waiting time and count to the existing inventory_phases diagnostic. Timing begins only after observing a full queue and ends after the wait predicate clears; values are buffered until candidate completion. The measured interval includes diagnostic hooks and lock reacquisition, so this is producer backpressure wall time, not a sum of kernel sleep time or worker CPU. Normal scale runs install no hooks. Queue size, worker count, serial quotas, checked opens, complete hashes and fallback behavior remain unchanged.

Formatting and wrapper syntax checks passed. Build chain root 65478 exited 0: all-target Clippy with denied warnings passed in 8.67 seconds, optimized library build in 86 seconds. Library SHA-256 E58FD7D6C78B68F8B9074B3D3495E3F2A311EE02FCB2C8CF7D1FA6A9FF3A3663. The existing deterministic full-queue test passed in 1.11 seconds and emitted one 514-microsecond wait, confirming that the new counter detects controlled producer blocking. No concurrent test executable build or benchmark ran during the scale measurement.

Owned scale run query-stage-eZlguU exited 0 with an empty/non-aborted Job (root 17700), no errors and complete SQL/immutable preservation. Ten hot queries retained zero Git starts, exactly two source captures and serialized snapshot equality after generated_at normalization. Discovery plus overlapping hashes averaged 92.0563 ms, of which producer backpressure averaged 12.1190 ms (1086 waits across ten queries). Remaining joins averaged 0.8068 ms and merge/sort 0.0264 ms. Subtracting the measured waits leaves about 79.9373 ms in the discovery path, including its filesystem checks, descriptor construction, submission and scheduling. This is not proof that all residual time is metadata I/O, nor a before/after optimization comparison.

The evidence directs further work toward the serial discovery/checking path rather than repeating the rejected eight-worker experiment. Any speculative metadata prefetch must preserve bounded resource usage, serial prefix classification/admission, complete hashes, cancellation and joined workers; it is not permission to skip ancestor checks or cache legacy byte validity. Evidence: target/verification/task6-inventory-queue-{clippy,build,positive}.log, task6-inventory-queue.log and task6-inventory-queue-statistics.json. Production remains db2abdf, and its actual CLI latency gates remain failed; this diagnostic does not establish acceptance.

### Serial directory and metadata check attribution

Added test-only cumulative timers around the existing walker directory validation and ordinary entry checked_metadata calls. The timers sum nanoseconds before converting to microseconds, keep call counts, and print once after candidate completion. Serial fallback has no profile attached. Operational bookkeeping, transition checks, read_dir iteration, root setup, descriptor construction and scheduling remain outside these two counters. Production execution and all safety checks are unchanged.

Build chain root 72366 exited 0: all-target Clippy with denied warnings passed in 8.81 seconds; optimized library build completed in 84 seconds. Formatting and wrapper syntax checks passed. Library SHA-256 CC90B43E37DF4284D8FE659FF392F036D79BE2926EC591B2DAB8E4C723B918E0. Owned scale run query-stage-yUNEhd exited 0 with empty/non-aborted Job (root 36304), no errors and complete SQL/immutable preservation. Ten hot queries retained zero Git starts, exactly two source captures and full serialized snapshot equality after generated_at normalization.

Per-query averages were 142 directory validation calls taking 43.7076 ms, and 622 ordinary entry metadata calls taking 17.9873 ms. Discovery plus overlapping hashes averaged 91.2393 ms, including 12.1781 ms producer queue waiting. These three nonoverlapping measured components leave about 17.3663 ms outside attribution. The directory timer wraps checked_canonical_directory, including its full ancestor metadata walk and canonicalization; it does not isolate the individual syscalls. This provides a concrete optimization target without weakening checks: investigate bounded speculative directory/metadata prechecks while preserving serial classification and quota admission, no payload I/O before admission, bounded pending descriptors, error ordering, cancellation, joined workers and serial fallback. Prior native-directory and eight-worker experiments remain rejected; these measurements do not justify repeating them or deleting checks.

Evidence: target/verification/task6-inventory-walk-{clippy,build}.log, task6-inventory-walk.log and task6-inventory-walk-statistics.json. These are diagnostic timings, not evidence of production acceleration or formal acceptance. Production db2abdf and actual CLI 13D8B43B remain unchanged, with latency and broader current acceptance gates still open.

## Retained db2abdf default idle resource acceptance

Root 55627 exited 0 using actual CLI SHA-256 13D8B43BEDE032A44FD8827916EC57059A45021B70B2116EF87E1C03D4F12395, fixed schema-2 scale receipt (20 worktrees, 100 sessions, 100000 events) and owned run shared-freshness-lvmr9O under the C-drive run parent. After four proxies completed ten warmup rounds and baseline verification, all proxies closed. The no-request native observation ran for exactly 600 seconds with 121 samples from 0 to 600 seconds. It began at 2026-09-13T07:09:05.795Z.

Owner PID 12612 exited normally during idle, code 0, signal null, no harness termination request; Node observed exit at 07:10:06.719Z. The retained native handle places its active interval at 60.7745594 seconds, bounded by the last-alive 60-second and first-exited 65-second samples. CPU counter delta was zero, hence both active-interval and full-window percentages are zero at counter resolution; this is not a claim of literally no execution. Peak sampled RSS was 24039424 bytes; reported lifetime peak was 25784320 bytes (24.58984375 MiB), including startup. Both CPU gates and the 150 MiB RSS gate passed.

Full SQL/immutable preservation passed after the window and after cleanup. Errors and cleanup errors are empty; all six retained child processes have terminal lifecycle records. The four proxies were deliberately closed before observation, while owner and observer exited with code 0. default_idle_resource_acceptance is true; continuous_residency_acceptance is false because the default owner exits, and overall performance_acceptance remains false. Full Rust regression overlapped setup and observation, so this is owner-only resource evidence, not an isolated whole-machine or four-proxy memory benchmark. Current cold/hot smoke and prior freshness failures remain open.

Evidence: target/verification/task6-db2abdf-resources-config.json, task6-db2abdf-resources.log and shared-freshness-lvmr9O/{report.json,resources.json} in the owned C-drive run parent. Both this root and full Rust root 50640 are now terminal; no owned command roots remain live from these runs.

### Test-only component-checked directory experiment

A new uncommitted test-only prototype retains the original ancestor/leaf checked_metadata, reparse rejection and is_dir checks but omits the final canonical path resolution in candidate inventory walking, where the returned PathBuf was unused. The serial oracle and production build remain unchanged. It has an explicit opt-in flag and reports per-query entry counts; this is not a production configuration option. Three focused tests compare nested/space/empty/parent paths, missing/file-ancestor errors and leaf/ancestor junction rejection against the original path. All 89 migration library tests passed with the experiment enabled in 96.37 seconds (build 13.88 seconds). All-target Clippy passed in 8.76 seconds; optimized build finished in 86 seconds. Build/test chain root 42693 exited 0.

After validation completed, ABBA root 74650 exited 0 using the same optimized test executable SHA-256 B89C981AEB56EADE349C70DE71FBF159691D85B66133321F1D7642E47A8135CD, retained by hash. Flag order was original/experiment/experiment/original; runs gXnnYy/lczoCx/QEz1zo/0tH1VG all exited 0 with empty owned Jobs, no errors and complete preservation. Twenty hot observations per side gave total means 197.2724 to 185.2059 ms and projection means 111.50535 to 101.10025 ms. Every experimental query recorded 142 component-directory calls; baseline queries recorded zero. Each run retained zero Git starts, exactly two source captures and full serialized snapshot equality after generated_at normalization. The two experimental run means were 181.4914 and 188.9204 ms; original runs were 193.1938 and 201.3510 ms.

This is preliminary same-binary diagnostic evidence, not actual CLI or formal latency acceptance. It supports further equivalence review rather than immediate production promotion. Rust documents different platform operations and non-exhaustive failure conditions for [canonicalize](https://doc.rust-lang.org/std/fs/fn.canonicalize.html) and [read_dir](https://doc.rust-lang.org/std/fs/fn.read_dir.html); the focused cases do not establish equivalence for all permissions, path syntax, filesystems or concurrent replacement. No complete atomic-directory snapshot guarantee is inferred. Retained production remains db2abdf and actual CLI 13D8B43B is unchanged.

Evidence: target/verification/task6-component-directory-{tests,clippy,build}.log, task6-component-directory-abba-{0,1,2,3}.log and task6-component-directory-abba-statistics.json. The prototype source is intentionally still uncommitted pending that review; no actual CLI uses it.

### Directory experiments not retained after equivalence review

The installed Rust compiler is 1.96.1. Review of its matching [Windows filesystem source](https://github.com/rust-lang/rust/blob/1.96.1/library/std/src/sys/fs/windows.rs) confirmed that metadata can fall back to cached directory information on some open failures, while canonicalization still requires a successful handle open and final-name query. Microsoft also documents [final-name query failures](https://learn.microsoft.com/en-us/windows/win32/api/fileapi/nf-fileapi-getfinalpathnamebyhandlew), including normalized SMB names and volume naming. Thus the omission prototype's normal-fixture gain was insufficient evidence of general error equivalence; it was not promoted. No actual failing ACL or special-filesystem fixture was manufactured, and no ACL was changed.

A second test-only variant kept every ancestor check and reused the checked leaf handle for metadata plus GetFinalPathNameByHandleW with the original normalized/DOS flags. It rejected non-directories/reparse leaves, retained original handling for terminal non-normal components/non-Windows, and bounded the final-name buffer with existing complete serial fallback on candidate errors. Two additional tests required final-name error propagation, verified growth/limits and compared the returned native canonical name. Chain root 57602 exited 0: 91 migration tests passed in 94.66 seconds (build 11.88 seconds), all-target Clippy passed in 8.99 seconds, optimized build completed in 89 seconds. Experimental SHA-256 C53124D9176D7E66A7681AF50F805C84D36FB107982B3FB3A00BEF9B99A0B99F is retained by hash.

After validation, same-binary ABBA root 35278 exited 0 with runs Wy8BSL/cKeBzx/SjzfWq/Zvl6zD. All owned Jobs were empty/non-aborted with no errors, zero exit codes and full preservation. Twenty hot observations per side gave totals 200.7390 to 195.7728 ms and projection 113.2008 to 109.9215 ms. Individual run means were 201.6729, 194.4453, 197.1003 and 199.8051 ms. Every enabled query entered the path 142 times, while baseline queries recorded zero; zero Git starts, two source captures and full within-run snapshot equality remained asserted.

The roughly 5 ms diagnostic improvement does not justify retaining the added Windows-specific handle/buffer machinery for this simplification task. Both variants were removed from active source; neither was used by an actual CLI. Patches and complete test-only module copies are preserved as task6-component-directory-omission-prototype.{patch,rs} and task6-directory-handle-rejected.patch / task6-directory-handle-rejected-prototype.rs. B89C981A and C53124D9 experimental libraries remain hash-named evidence only. The default optimized library was restored and verified as the retained db2abdf 389FE402 baseline (this older binary does not contain later observer/inventory diagnostics); it is not claimed to be a fresh c5c2097 diagnostic build. Current tracked source is restored, and actual CLI 13D8B43B remains unchanged.

Evidence: task6-directory-handle-{tests,clippy,build}.log, task6-directory-handle-abba-{0,1,2,3}.log and task6-directory-handle-abba-statistics.json. No owned command roots remain live. Current full regression and compatibility evidence for retained production remains valid; cold/hot/freshness and remaining full-goal gates remain open. Subsequent work should address larger cold/freshness costs rather than repeat either directory prototype as an untested direction.

### Cold projection attribution on retained production

The default same-core diagnostic on retained library 389FE402 (run query-stage-CPBEFa, root 32344, exit 0) recorded one initial sealed query at 3198.523 ms with 59 Git starts. Its separately measured component queries are not an additive decomposition of that observation.

The new opt-in cold_projection_phases test mode instead records six consecutive phases inside each first application projection. Three fresh QueryHarness instances each execute an initial query followed by a controlled hot replay. Replay must start zero Git processes, capture source configuration exactly twice and equal the complete first ApplicationSnapshot after normalizing only model.generated_at. The 60-second replay age is test-only; production freshness policy is unchanged.

All-target Clippy passed in 12.48 seconds and the optimized library build completed in 90 seconds. Owned run query-stage-Dl5kI4 used library SHA-256 19811DCC022F2B9EACCAA43D75DCEEC8CEF1ADC86E0E5190FA783FB4B02F3C4F and the unchanged scale receipt. Wrapper root 80892 exited 0; owned Job root 23696 exited 0, empty confirmed, non-aborted, errors empty, full SQL and immutable inventory preservation passed. All three cold/replay comparisons passed.

Initial query times were 3321.306, 3229.465 and 3295.247 ms, each with 59 Git starts. Mean total was 3282.006 ms. Mean nested phases were source/anchor 2.499 ms, storage read 965.647 ms (one Git start), collection 1833.326 ms (51 Git starts), client preparation 1.324 ms, model projection 6.772 ms and history/snapshot 0.015 ms. The remaining outer-query time was 490.285/456.323/470.660 ms and includes work outside this projection, not an identified single component. Buffered diagnostic output overhead is included in the outer total.

These are three fresh in-process application queries with OS caches uncontrolled; they exclude process startup, IPC setup and harness construction. They are neither formal cold-start acceptance nor evidence of acceleration. Production and actual CLI 13D8B43B are unchanged. The next useful split is within the roughly 966 ms cold storage read using the existing storage phase boundaries; collection remains another large cost. Evidence: task6-cold-projection-build.log, task6-cold-projection.log and task6-cold-projection-statistics.json under target/verification.

### Nested cold storage attribution

The same test-only mode now enables existing storage boundaries only outside a query epoch. The wrapper requires exactly six storage phases per cold query, ordered stage names, nonnegative counters, total storage Git starts equal to the enclosing projection stage, and summed storage timings no greater than that stage. All prior cold/replay and preservation assertions remain.

Clippy passed in 12.09 seconds; the optimized build took 91 seconds (root 22535, exit 0). Scale run query-stage-vwUbrr used library SHA-256 4E008FB5BEBDEE5AF50939AB6E9CA97DEB3B230595095A42A96D5B6162C06FF5. Wrapper root 3352 exited 0; Job root 16852 exited 0 with empty confirmation, no abort/errors and full SQL/immutable preservation. All three complete snapshot replay comparisons passed.

Mean same-core cold query was 3264.554 ms. Within storage, mean database path/identity checks took 12.054 ms, snapshot/schema 0.295 ms, origin observation 352.497 ms (one Git start), input loading/verification 607.284 ms, origin qualification 1.959 ms and commit/cache 0.071 ms. These are nested diagnostic observations, not process-startup acceptance or an optimization comparison. Production and actual CLI remain unchanged.

Input loading includes presence, session journal summaries, routes and bindings; the 607 ms must not be attributed entirely to parsing without another measurement. Source inspection nevertheless identifies a bounded candidate: parse_record builds canonical typed JSON twice, once for full-record canonical-byte equality and again for the unsigned hash. Reusing the checked canonical value after removing only the top-level sha256 field could avoid duplicate tree construction. Any such change must preserve typed deserialization, canonical key ordering, floating-point refusal, exact unsigned bytes, malformed/corrupt rejection and error precedence against the original parser. It is not implemented or proven by this diagnostic.

Evidence: target/verification/task6-cold-storage-build.log, task6-cold-storage.log and task6-cold-storage-statistics.json. Current formal latency/freshness and remaining goal gates are still open.

### Retained canonical journal tree reuse

Journal parsing now serializes the typed record to a JSON value once, retains floating-point refusal and recursive canonical sorting, and compares the full encoded bytes exactly as before. It then removes only the top-level sha256 member and hashes the encoded unsigned value. Nested payload digest fields remain intact. Typed deserialization, record size limits, canonical rejection before digest rejection and all chain validation remain unchanged. This removes duplicate tree construction without adding a cache, service or thread pool.

A frozen copy of the original parser is the test oracle. Tests cover all 19 event types, empty/nested payloads, Unicode/escapes, integer extremes, nested sha256 keys and both null/non-null previous digests. Each checks exact unsigned bytes against UnsignedJournalRecord and complete parsed results; malformed JSON, invalid UTF-8, duplicate/unknown/missing fields, wrong types, altered hashes/sequences, whitespace/noncanonical formatting and oversized input compare exact debug-form errors. The journal subset passed 12 tests in 1.37 seconds, including streaming-summary corruption and worker lifecycle tests. Four integration suites passed 37 tests: canonical 6, final-review journal 5, journal flow 13 and SQLite journal 13 (root 77101, exit 0). All-target Clippy passed in 12.27 seconds; optimized build took 94 seconds (root 22982, exit 0). An initial generated test-file encoding error was corrected before these successful runs.

After both build and integration roots terminated, ABBA root 99209 exited 0 on the unchanged scale fixture. Baseline library 4E008FB5 and candidate C0D743F3CB6B07316125E5061B6DD4475F38815A292814333F5594B124B3F360 ran in order xwLWd1/Qpkpgx/SAdHVC/jDQJDA. All four Jobs were empty/non-aborted, exit 0, errors empty, with complete SQL/immutable preservation and three cold/hot snapshot roundtrips per run. Six cold observations per side gave mean input-loading/verification 612.574 to 484.942 ms, and mean whole same-core query 3262.720 to 3171.546 ms. Origin observation was 351.564 to 349.584 ms; collection was 1820.665 to 1852.240 ms. The input-stage reduction is about 127.633 ms (20.8%), while whole-query reduction is about 91.175 ms (2.8%); these are diagnostic means, not formal process-startup or p95 acceptance.

The small production change is retained on this evidence. Actual CLI 13D8B43B has not been rebuilt and still represents db2abdf. Previous full regression, real browser/host and resource results therefore remain historical baseline evidence rather than proof of the new candidate. Next steps include an actual CLI build/smoke and renewed candidate acceptance as appropriate; overall goal remains open.

Evidence: target/verification/task6-canonical-reuse-{tests-expanded,clippy,build,integration}.log, task6-canonical-reuse-abba-{0,1,2,3}.log and task6-canonical-reuse-abba-statistics.json. The old library is retained under its full hash name in the build directory.

### Actual CLI smoke after canonical tree reuse (98205f5)

Release CLI build root 65503 exited 0 after 70 seconds. The isolated C-drive candidate copy at target/verification/task6-candidate-98205f5/devmap.exe has SHA-256 E1C7C4353ABB30FF0DC5B321B027D724A2FA41DA69E11E9DAA52202C1FC54A85. Smoke root 36282 exited 0 using the unchanged scale receipt and run shared-summary-4KCqIx. Both build and benchmark roots are terminal.

Two owner-process cold observations were 4216.024 and 4126.309 ms. Owner readiness took 228.141/225.445 ms, client initialization 284.799/281.045 ms, and first request 3676.180/3591.246 ms; small residual intervals are outside those timers. Four clients with three measured hot requests each gave p95 255.480/256.189/250.078/251.400 ms. Maximum result size remained 8781 bytes. These low-count observations fail both cold and hot thresholds; summary_latency_acceptance and performance_acceptance are false. They are not a controlled old/new CLI comparison and do not establish a warm-query regression or formal improvement over historical runs.

Read-cohort preservation passed, final SQL state matched and immutable inventory was rechecked by the harness. Errors and cleanup errors were empty; all nine owned children have terminal harness-stop lifecycle records. The single small-change diagnostic detected the change in 3821.194 ms and restoration in 3851.338 ms; it is not the 100-change freshness acceptance population. The source fixture was restored. Actual program latency, full regression, browser/host and resource acceptance for the new production candidate remain open.

Source inspection identifies the next bounded candidate: DockReducer::reduce_with_tasks already overlaps topology and relationship reads, while DockProjectionContext::collect still runs them sequentially after the opening worktree/topology boundary. Consider using the existing budget-preserving, joined probe pair to overlap topology with configuration-plus-relationships while retaining both closing consistency checks, original ordinary-error priority and fatal supervision. The potential saving is limited by the shorter topology pipeline and is not yet measured. Existing collection phase labels must be updated if their execution overlaps; do not present overlapping intervals as additive.

Evidence: target/verification/task6-98205f5-cli-build.log, task6-98205f5-cdrive-smoke-config.json, task6-98205f5-cdrive-smoke.log and shared-summary-4KCqIx/report.json under the owned run parent.

### Retained shared collection pipeline overlap

DockProjectionContext now uses the existing probe pair for topology and configuration-plus-relationships on the same worktree inventory and absolute Git budget. The helper joins started work, falls back to serial order on thread-creation refusal, preserves primary ordinary-error priority and propagates fatal Git supervision errors. Opening/closing worktree and topology checks, route-target checks and model construction remain intact. No cache or permanent worker is added.

The new serial-oracle test passed in 16.81 seconds after a 15.46-second build (root 87162, exit 0). It compares full worktree descriptors, topology, relationships and configuration keys across main/linked clients, four development-target configurations and intervening dirty-file changes. Five probe-supervision tests passed. Integration root 43262 exited 0: three origin/reanchor tests passed in 25.36 seconds and ten repository-application tests in 51.54 seconds. Clippy passed in 12.62 seconds and optimized compilation in 100 seconds (root 98481, exit 0). These 19 tests are focused evidence, not full acceptance.

After both roots terminated, ABBA root 48644 compared baseline C0D743F3 and candidate 5112147082A6004DCE87083CEFDDE4611B4B1A0768E5F8C4A59BB23C32C5A9F1 in ECZVdC/Pcc4Zg/f3XdHf/Roa4Bn. Six same-core cold observations per side gave mean collection 1834.860 to 1687.474 ms and complete query 3222.097 to 3092.998 ms. Input loading was 525.358 to 528.991 ms and origin observation 378.834 to 369.477 ms. All runs passed SQL/immutable preservation and complete cold/hot snapshot comparisons; Jobs were empty, non-aborted and error-free. The 147 ms collection and 129 ms query reductions justify retaining the change; they are not process-startup or p95 acceptance.

Collection diagnostics now use one topology_and_relationships stage, five total stages instead of seven. Candidate run PHvoLx passed three stable full-collection comparisons, preservation and Job checks, with totals 1702.411/1726.270/1694.611 ms. Nested relationship Git counters include simultaneous topology starts because the counter is process-wide: do not attribute them exclusively to relationship commands or add them to parent counts. Parent and child timings overlap. Root 48644 exited 0 after this run.

Actual CLI E1C7C435 still contains preceding production 98205f5. Full candidate Rust/browser/host/resource and formal latency/freshness gates remain open. Evidence: target/verification/task6-collection-overlap-{tests,supervision,clippy,build,application}.log, task6-collection-overlap-abba-{0,1,2,3}.log, task6-collection-overlap-abba-statistics.json and task6-collection-overlap-phases.log.

### Actual CLI smoke after shared collection overlap (fb3d5e5)

Release build root 51954 exited 0 after 72 seconds. Actual candidate SHA-256 F404A756F6DBD8A41A1523F0FCD65BC94D20419D4C6F327EF525A8B446C5F83D is copied to target/verification/task6-candidate-fb3d5e5/devmap.exe. Smoke root 68239 exited 0, run shared-summary-JQwzd2, unchanged scale fixture and owned C-drive run parent.

Cold observations were 4182.234/4129.635 ms. Owner readiness was 298.629/229.014 ms, client initialization 269.161/264.866 ms and first request 3585.586/3609.316 ms; the remaining interval is outside those component timers. Four clients, three measured hot requests each, gave p95 250.067/246.695/249.042/245.412 ms. Maximum result remained 8781 bytes. Both gates failed. These low-count observations are not a controlled CLI comparison and cannot establish the size of an end-to-end gain.

Read-cohort preservation and final SQL/immutable assertions passed. Errors and cleanup errors were empty; all nine owned children have terminal harness-stop records. Single small-change and restoration observations were 3759.546/3768.037 ms, with the fixture restored; this is not formal freshness acceptance. Overall performance acceptance remains false. Full candidate Rust/browser/host/resource verification remains pending.

Next bounded investigation: after merge-base validation, relationship_for_observed currently reads tag metadata, commit metadata and rev-list counts serially. Their inputs are available together. Consider overlapping these existing reads for the immutable shared representative, preserving typed output checks, ordinary error order, tag witnesses and fatal supervision. Do not infer zero counts merely from equal object IDs: omitting a Git command would require separate error-equivalence evidence. Source inspection also found client startup includes SourceGitInspector plus runtime identity acquisition; their measured contribution is not isolated here.

Evidence: target/verification/task6-fb3d5e5-cli-build.log, task6-fb3d5e5-cdrive-smoke-config.json, task6-fb3d5e5-cdrive-smoke.log and shared-summary-JQwzd2/report.json. No owned roots remain live.

### Retained immutable representative probe overlap

After merge-base validation, the shared immutable representative now overlaps existing tag, commit-metadata and symmetric-count reads through nested existing probe pairs. Each closure validates output before returning, retaining tag-before-metadata-before-count ordinary error priority and fatal Git supervision. Raw tag witnesses, tag sorting/limits, metadata and distance logic remain. Ordinary per-worktree calls stay serial. No Git command is omitted.

Root 10244 exited 0: seven shared-facts tests passed in 29.19 seconds after a 15.46-second build. They compare target, integration branches, relationship rows and warning code/worktree pairs against original computation, and cover graph shapes, aliases, Unicode, tag limits, tag changes, ordinary failure and real deadlines. Five probe-supervision tests passed; Clippy passed in 13.39 seconds (root 48737, exit 0). Optimized build root 61342 exited 0 after 93 seconds. These 12 focused tests do not replace full regression.

After all roots terminated, ABBA root 41929 exited 0, baseline 51121470 and candidate 0B315CB12153F658C38A3ED83D8A028AFE64AE56768303F1469A177C175A3999 in 9fIP0g/d0tjnq/dnogHu/D0R2hO on the unchanged scale fixture. All Jobs were empty/non-aborted, exit 0, errors empty, with SQL/immutable preservation and cold/hot snapshot equality. Six cold observations per side gave mean collection 1714.380 to 1588.986 ms and complete same-core query 3119.450 to 2933.134 ms. Input loading varied 528.573 to 506.236 ms and origin observation 374.756 to 356.841 ms; the entire 186 ms total difference cannot be attributed solely to overlap. The approximately 125 ms collection reduction supports retention.

The below-three-second in-process mean excludes process startup, IPC connection and harness setup. It is not cold-start acceptance. Actual CLI F404A756 still contains preceding fb3d5e5. Full candidate regression/browser/host/resource and formal latency/freshness gates remain open. Evidence: target/verification/task6-representative-overlap-{tests,supervision,clippy,build}.log, task6-representative-overlap-abba-{0,1,2,3}.log and task6-representative-overlap-abba-statistics.json.
