# Open task：排查记录与用户要求的补丁撤回

日期：2026-09-05。最新状态：**补丁已按用户要求撤回**。用户指出“是你的虚拟鼠标问题，把这部分改回到早上修改前的上一个版本”。已恢复页面源码及对应测试，未动其他开发结果。自动化限制不等于真实用户点击故障，先前的宿主故障归因不足以确诊，予以撤回；本轮没有新的原生跳转测试或根因实测。

源码SHA256恢复为 `4920e6254a952fd5405c927176a455930bba75f81a4e04f3b0e9aff4a5d4a3f8`，与补丁前构建指纹一致。下文保留历史排查和测试记录，不代表当前结论或仍存在的改动。该补丁从未部署到右侧页面，故无需重装或重启。

## 用户证据与只读排查

- 用户先确认一次“可以跳转”，后报告“突然点击opentask又没效果了”。最新补充：“opentask右侧点不进去。但是链接复制出去，默认浏览器可以”。后续失败优先于此前单次通过。
- 当前页面标题仍为 `DevMap · Rail View — Repository topology`；不是旧版 Git Work Map。
- 当前实际浏览器 DOM：`window.openai` 和 `window.openai?.sendFollowUpMessage` 均不存在。任务为 `codex://threads/<verified-id>` 链接，`target="_blank"`，`rel="noopener"`。
- 源码在该浏览器分支只依赖链接默认行为，点击回调就宣告“request sent”；没有收到宿主回执，不能确认投递或到达。
- 浏览器清单中曾观察到一个标题为 New tab、地址为 Codex task 协议的标签页。不能据此推断具体失败点击、所有失败原因或已经到达原生任务。
- 用户对相同链接的跨浏览器对照缩小了故障边界：链接至少可通过默认浏览器到达任务，问题集中在内置 Browser Dock 向宿主交接。尚未证实具体是新标签页处理、外部协议策略还是宿主事件分派；不靠猜测更改 target、私有桥接或系统协议设置。

## 接口证据边界

[OpenAI 官方插件接口文档](https://developers.openai.com/plugins/reference) 将 `sendFollowUpMessage` 定义为发送组件消息，将 `openExternal` 定义为外部链接接口；它们不是这个普通 localhost 页面中自动存在的任务导航接口。已读取的官方文档未建立此 Browser Dock 对 `codex://threads` 的稳定支持承诺。不能把工具层的任务导航能力假定为网页 JavaScript 能力。

此前浏览器自动化已明确阻止协议链接激活。本轮只读检查，不使用其他浏览器、命令行、原生注入、私有接口或导航工具绕过该限制。

## 历史源码修正（从未部署，现已全部撤回）

- 无宿主回执时不再宣告“request sent”，改为说明无法确认是否打开，并提示默认浏览器或 Codex 侧栏的手动恢复方式。
- 现有任务详情提供 Copy task link 和可手选的完整链接；仅使用现有本地、来源校验和任务 ID 校验通过的链接。
- 复制操作只写用户剪贴板，不启动浏览器或导航；复制能力不可用时明确提示手动选择，不谎称成功。
- 保留原有链接、目标、协议及正常用户点击行为；不通过换路由伪装修复。

测试先出现3个预期失败，再通过源码修正使114个 Node 测试全通过；覆盖无确认提示、刷新不擦除提示、精确链接复制及剪贴板不可用恢复。相关 Rust 测试35项通过（dock_mcp 16、dock_ui_contract 13、dock_viewer 6），exit 0。Node DOM 适配器不能证明真实原生任务跳转、浏览器剪贴板授权或物理布局。此前252项全量 Rust 结果属于前一构建，本次未重复全量运行。

运行中的 PID21492 仍是此前已记录的候选，不包含本次源码变更。没有重装 skill、替换当前页面、commit、push、merge 或修改 Windows 动画设置。原生右侧跳转仍需要受支持的宿主集成及真实用户验证；不把此恢复补丁算成完整修复。
